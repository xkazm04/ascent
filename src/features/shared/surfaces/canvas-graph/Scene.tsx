"use client";

// STUB scene for canvas-graph — authored by the /surface run; this placeholder only satisfies the body contract.

import type { SurfaceSceneProps } from "../surfaceBody";

export function Scene(_props: SurfaceSceneProps) {
  return <p className="type-caption text-slate-500">Scene for canvas-graph is being authored.</p>;
}
