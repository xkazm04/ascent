"use client";

// STUB scene for diff-comparison — authored by the /surface run; this placeholder only satisfies the body contract.

import type { SurfaceSceneProps } from "../surfaceBody";

export function Scene(_props: SurfaceSceneProps) {
  return <p className="type-caption text-slate-500">Scene for diff-comparison is being authored.</p>;
}
