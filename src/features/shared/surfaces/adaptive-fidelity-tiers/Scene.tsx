"use client";

// STUB scene for adaptive-fidelity-tiers — authored by the /surface run; this placeholder only satisfies the body contract.

import type { SurfaceSceneProps } from "../surfaceBody";

export function Scene(_props: SurfaceSceneProps) {
  return <p className="type-caption text-slate-500">Scene for adaptive-fidelity-tiers is being authored.</p>;
}
