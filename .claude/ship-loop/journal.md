# Journal (append-only, one line per event)

2026-07-05 BOOT started (FRESH, ascent). Prior .claude/ship-loop state was for a different app (ai-bookkeeper) → archived to archive-ai-bookkeeper/. Repo=ascent, branch=master. User decisions: fresh boot on ascent; full boot (gate + full multi-lens audit).
2026-07-05 NOTE: no ship-loop SKILL.md/command exists anywhere in this repo or ~/.claude; procedure reconstructed from archived state-file conventions (BOOT→GATE→AUDIT→9-dim scorecard→append-only backlog→milestones→checkpoints w/ auto-decide reversible + defer existential).
2026-07-05 NOTE: `npm run gate` (scripts/maturity-gate.mjs) is an ascent PRODUCT feature (gates external repos via /api/gate), NOT the build gate. Build gate = tsc --noEmit + eslint + vitest run + next build.
2026-07-05 NOTE: large uncommitted WIP in working tree (~40 modified + many new files/dirs incl integrations/, team-standings, ai delivery, practices, securityRegisterShared, 2 new prisma migrations). Auditing working-tree state AS-IS.
2026-07-05 GATE(boot): typecheck ✓ 0 | build ✓ (~60 routes, all dynamic) | lint ✗ 35 problems (22 errors, 13 warn) | tests ✗ 4 failed / 2881 (183/186 files). CAVEAT: bash `echo EXIT:$?` masked real codes in task-notifications (all showed "exit 0"); truth is in EXIT: lines.
2026-07-05 GATE lint: 22 errors dominated by React-19 hooks-purity rules (eslint-config-next 16.3 elevated to errors) — setState-in-effect / create-components-during-render / impure-fn-during-render / access-refs-during-render. Files: PassportTable.tsx (8×), SecurityRiskRegister.tsx (5×, WIP), Modal.tsx (2), DevInspector, FleetMap, CreditsControl, CreateIssueModal. Plus prefer-const (liveWarRoomFold.test.ts:188), no-explicit-any (org-insights.test.ts:116,139). Build passes because next build doesn't run these as blocking.
2026-07-05 GATE tests: 4 fails, ALL WIP-integration breaks (not deep bugs): (1) org/scan/route.test.ts — `@/lib/db` mock missing new `persistTeamStandings` export → SSE errors; (2+3) org/import/route.test.ts credit-cap slice returns undefined (result shape/behavior drift); (4) usage/AllotmentPanel.test.ts — Free allowance 5 vs 10 drift (code returns 5, test expects 10). → Milestone-1 candidates (mechanical), but #4 needs a product call on the real allowance number.
2026-07-05 AUDIT tests lens done → 🟡. ~184 unit specs + 9 e2e; scan-metering + authz + credits exceptionally covered. Top holes: Polar billing/webhook route (order.paid/refunded fulfilment) ZERO tests [C]; clawbackOrderRefund (credits.ts:200) refund-abuse defense untested [C]; checkout route guards untested [H]; db/integrations recordUsage+rollup (feeds /delivery $ ROI) untested [H]; integrations/ingest route untested [H]; team-standings persist [M]; e2e NOT in CI [M].
2026-07-05 AUDIT architecture lens done → 🟡. Typecheck green, deleted onboarding comps have ZERO dangling refs, migrations consistent, securityRegisterShared no dup, layering clean (db imports type-only from UI). Debt: 11 .tsx >300 LOC (contradicts AGENTS.md "zero" — InstallationRepos 529, OnboardingFlow 500, plan/Simulator 492, usage/page 485...) [M]; OnboardingChecklist NAME COLLISION (onboarding/ vs onboarding/tour/) [M]; context-map.json stale (7 files unmapped incl securityRegisterShared, tracklight, security/checks+exposure) [M]; 3 large .ts modules >880 LOC (org-insights 937, scans-read 913, analyze/index 883) need barrel split [M]. TODO density low.
2026-07-05 AUDIT UX lens done → 🟢 strong foundation. Exemplary: ~6-min scan progress UX (ReportClientStatus), Modal focus-trap, non-blocking tour, TILE_LEDGER discipline, WIP surfaces have proper empty/error states. Gaps: route boundaries missing on ~31/35 segments (only 4 have loading/error) [M]; 300-LOC violations (cross-confirms architecture, 10 files) [H]; ConstellationField.tsx:174 SVG <title> mixed-children hydration bug [L]; a few unlabeled inputs / no aria-live on copy [L].
2026-07-05 AUDIT security lens done → 🟢 strong defense-in-depth. NO cross-tenant IDOR (org layout.tsx:82 canReadOrg gates all 21 pages via real Membership), all 64 API routes gated, SSRF closed (source.ts host allowlist), cmd-injection closed (claude-cli stdin), ASCENT_AUTH_BYPASS fail-closed in prod (env.ts:30), Polar webhook + crons fail-closed, secrets clean (only .env.example tracked), getViewer-in-SSE handled. 3 low/info only: ingest-token dev-secret fallback (ingest-token.ts:15) [L]; narrative prompt-injection latitude (prompt.ts:157) [L]; public-scan quota fails-open by design [info].
2026-07-05 CP0 done (user PRESENT): ship bar="just keep improving" (severity order correctness>money-in>polish>strategy); M1="strategy first" (dim-9 decisions 28-32 → docs/VALUE-CASE.md → drive calls); cadence=continuous (stop only for blockers/product-decisions); allowance=5/mo. 32-item backlog seeded. Scorecard: 1🟢 2🟡 3🔴 4🟡 5🟡 6🟢 7🟢 8🟡 9🟡.
2026-07-05 M1 start (strategy): verifying load-bearing claims (scoring blend/guardband, gate mock-default, claude-cli temp) before writing the decision brief.
2026-07-05 VERIFY (code, mine): engine.ts guardband real (LLM clamped ±band of signal :115; D9 deterministic :118; discrepancy widen ×2 :114 landed). gate/route.ts:38 deterministic-by-default + reads/writes mock key to kill stochastic-cache (:76-81). temp: gemini/openai/bedrock use envNumber("LLM_TEMPERATURE",0.2) → temp-0 is an env flip; claude-cli has NO temp knob (the reference scan's non-reproducibility source).
2026-07-05 CP1 done (M1 strategy): D28 two-tier / D29 gate-only-reproducible / D30 validate-then-broaden / D31 platform-eng-leader buyer / D32 no-synthetic-headline-numbers. docs/VALUE-CASE.md written. Backlog 28-32 ☑, derived 33-38 filed, 12-14 elevated. M1 COMPLETE (no code).
2026-07-05 M2 start (green the gate, continuous cadence): fixing 4 failing tests then 22 lint errors.
2026-07-05 M2 tests ☑: added persistTeamStandings mock to org/scan + org/import route tests; AllotmentPanel free-included 10→5 (per D3). Full suite 2881/2881 (was 2877/4-fail).
2026-07-05 M2 lint ☑: 22 errors → 0 (13 benign warnings remain, pre-existing). Real fixes: hoisted in-render `Th` → module-scope component in PassportTable (7) + SecurityRiskRegister (5); Modal latest-ref assignment during-render → post-commit effect. Scoped eslint-disable w/ justification for 6 intentional hooks patterns (setMounted mount-gate ×2, fetch-on-open, focus→expand+scroll, draft-sync, Date.now in async handler) + 2 no-explicit-any on prisma test doubles + let→const.
2026-07-05 M2 GATE re-run: tests ✓ 2881/2881 · lint ✓ 0 err · build ✓ · tsc ✗ 3 errors in GENERATED .next/types/validator.ts (/api/dev/seed-ai-usage, integrations/ingest/v1/logs, /v1/metrics "does not satisfy AppRouteHandlerRoutes"). NOT from M2 edits (no route touched). Boot tsc missed these — it raced the concurrent boot build that rewrites validator.ts. Route files all EXIST. Diagnosing: inconsistent .next/types from concurrent tsc-vs-build; running clean build→sequential tsc to confirm it's a staleness artifact vs real.
2026-07-05 M2 GATE resolved: `rm -rf .next && npm run build` (exit 0) THEN sequential `tsc --noEmit` = exit 0, ZERO errors. The 3 validator.ts errors were a stale/inconsistent .next/types artifact from running tsc CONCURRENTLY with build. GATE METHODOLOGY FIX: always run tsc AFTER build finishes (sequentially), never in parallel — next build rewrites .next/types/validator.ts mid-flight.
2026-07-05 M2 ☑ COMPLETE — gate GREEN: tsc ✓ 0 · lint ✓ 0 err (13 benign warn) · tests ✓ 2881/2881 · build ✓. Backlog 1,2,4 done; 3 done (test aligned to 5/mo). Changes live in working tree (mixed into the pre-existing large WIP — NOT committed; commit needs user since the tree is a WIP soup).
2026-07-05 M3 start (user: "proceed with M3"): AI-delivery synthetic-$ disclosure (items 12,13,14). Reading delivery/ai/* + ClaudeCodeSetup + ingest routes.
2026-07-05 M3 VERIFY: /v1/metrics/route.ts DOES parse OTLP/JSON + persist per-repo (recordUsage mode:add, :39-41) → the "persistence ships next" copy was wrong. seed-ai-usage/route.ts EXISTS (contra boot functional lens) → item 15 is context-map staleness only (filed 40).
2026-07-05 M4 start (user: "proceed with M4"): money-in test holes (items 5,6,7 — Polar webhook route, clawbackOrderRefund, checkout guards). Reading billing/webhook + checkout routes, credits.ts, credits.test.ts pattern.
2026-07-05 M4 tests written (source untouched — pure test additions): item 6 +9 clawback tests (credits.test.ts 25→34, scoped run green); item 7 checkout/route.test.ts 9 tests (green); item 5 webhook/route.test.ts 14 tests (green) via vi.hoisted capture of the @polar-sh/nextjs Webhooks() config + a 503 fail-closed re-import. +32 money-in tests total.
2026-07-05 M5 start (user: "proceed with M5"): integrations tests (items 8 db/integrations recordUsage+rollup, 9 ingest route, 10 team-standings persist). Reading integrations.ts + team-standings.ts.
2026-07-05 M5 tests written (source untouched): item 8 integrations.test.ts 11; item 9 ingest/route.test.ts 6 (initial bug: helper named `post` shadowed the intent — was calling the request-builder not the route POST → res.status undefined; renamed to mkReq + wrapped in POST()); item 10 team-standings.test.ts 10. +27 tests, scoped runs green.
2026-07-05 M6 start (user: "M6 polish boundaries/300-LOC"): scoped to boundaries (item 23) — DRY the near-duplicate root+org error.tsx into a shared RouteError, add loading.tsx skeletons to heavy top-level pages (usage, report/[owner]/[repo], report/compare, portfolio, leaderboard). NOTE Next semantics: org sub-segments already covered by org/[slug]/error.tsx (renders in-shell w/ OrgNav) + root error.tsx catches the rest, so the real gap is loading shells (only 2 segments had them). 300-LOC extraction (18) deferred to M7 (bigger/riskier — 11 files).
2026-07-05 M6: created shared RouteError + PageSkeleton (components/ui); refactored root error.tsx + org error.tsx to delegate to RouteError; added 5 loading.tsx re-exports. error.test.ts broke (5 fails) — it walked AppError's returned tree directly, which is now a single <RouteError> element; fixed tree() to descend one level through the delegated component (behavior-preserving, assertions unchanged) → 9/9.
2026-07-05 M7 start (user: "M7 300-LOC extraction"): item 18 — restore AGENTS.md ≤300-LOC invariant across 11 .tsx. Fanning out 1 extraction agent per file (independent: each creates co-located files + edits only its target), then a hard full gate. Targets: InstallationRepos 529, OnboardingFlow 500, plan/Simulator 492, usage/page 485, RepoSegmentsPanel 421, FleetMap 376, ReportView 334, ScanModal 328, SkillsPanel 318, BacklogItemRow 312, TrendChart 303.
2026-07-05 M7: 11 parallel extraction agents all completed (pure relocation, co-located files, exports/eslint-disables preserved). New LOC: InstallationRepos 136, OnboardingFlow 132, Simulator 293, usage/page 143, RepoSegmentsPanel 285, FleetMap 232, ReportView 223, ScanModal 252, SkillsPanel 236, BacklogItemRow 292, TrendChart 240. ~30 new co-located files. NOTE: agents saw transient tsc errors (OnboardingFlow `byProminence`, usage `UsageDashboard`) from reading each other's mid-flight state — final assembled tree is clean.
2026-07-05 M8 start (user: "M8 quick UX 24+25"): item 24 (ConstellationField SVG <title> mixed-children hydration) + item 25 (NewPracticeModal unlabeled inputs, ClaudeCodeSetup CopyButton aria-live). Scatter keyboard-access sub-item deferred (larger, UX-lens said acceptable-as-is).
2026-07-05 M9 start (user: "M9 value execution 36/37/38"): 36 (live 10-org re-scan) needs LLM+GH infra — not reliably runnable in-session; 38 (repricing) needs user pricing decisions. Tractable = item 37 (broaden GH-native-biased signal layer, the golang-floor fix) validated by a deterministic regression test (durable substitute for the flaky live re-scan; fits D29). Investigating root cause + risk before changing scoring.
2026-07-05 M9 FINDING (Explore agent): item 37 is ALREADY DONE — all the audit's P0/P1 broadening landed + is tested (D3 Gerrit/bors/Buildkite off-GitHub CI + inline guardrails, D6 off-platform review, D9 null-not-0 for GH-native checks, D4 dep-bot fallback, P1-1 guardband). Backlog assumption (37=TODO) was stale. So M9's real work = item 36's VALIDATION, done deterministically.
2026-07-05 M10 start (user: "M10 context-map refresh"): item 20 — reconcile context-map.json to the tree (M7's ~30 extractions + M3/M6 additions + earlier-unmapped securityRegisterShared/tracklight/security-checks+exposure). Computing drift first.
2026-07-05 M9 ☑ COMPLETE — added signals.test.ts "golang-floor regression" (3 tests): D3 credited ≥35 for an off-GitHub golang/go-style repo (was ~1); un-flooring lifts the overall; the GitHub-Actions twin scores within 8 pts (was the 20-vs-74 chasm). Thresholds held first try. GATE GREEN: lint ✓0 · tests ✓2943/2943 · tsc ✓ (build unchanged — test-only). Item 37 ☑ (already-done), 36 ◐ (deterministic validation done; live re-scan = user task), 38 deferred (needs pricing decisions). 9 milestones stacked uncommitted.
2026-07-05 M8 ☑ COMPLETE — item 24 done, item 25 mostly (scatter keyboard access deferred). ConstellationField <title> single-child; 4 aria-labels on NewPracticeModal fields; aria-live span on CopyButton. GATE GREEN: lint ✓0 · tests ✓2940/2940 · build ✓ · tsc ✓. 8 milestones stacked uncommitted.
2026-07-05 M7 ☑ COMPLETE — item 18. AGENTS.md invariant RESTORED: PowerShell check = zero .tsx >300 LOC. GATE GREEN: lint ✓0 · tests ✓2940/2940 · build ✓ · tsc ✓ (sequential, whole tree). 7 milestones stacked uncommitted.
2026-07-05 M6 ☑ COMPLETE — item 23 (slice). GATE GREEN: lint ✓0 · tests ✓2940/2940 · build ✓ · tsc ✓. 6 milestones stacked uncommitted — commit strongly recommended.
2026-07-05 M5 ☑ COMPLETE — items 8,9,10. GATE GREEN: lint ✓0 · tests ✓2940/2940 (+27) · build ✓ · tsc ✓. Integrations $ ROI storage + ingest auth/validation + team-standings persistence now covered. 5 milestones stacked uncommitted — recommending a commit.
2026-07-05 M4 ☑ COMPLETE — items 5,6,7. GATE GREEN: lint ✓0 · tests ✓2913/2913 (+32) · build ✓ · tsc ✓ (sequential). Money-in path (Polar webhook fulfilment, refund-abuse clawback, checkout guards) now covered — was ZERO. 4 milestones now stacked uncommitted.
2026-07-05 M3 ☑ COMPLETE — items 12,13,14. Threaded org `slug` delivery→module→both views. Ledger: $-tiles locked + verdict-line real-facts-only + per-row $/seats/tool dashed when simulated. Quadrant: "sample spend" watermark + $/seats stripped from tooltip/hover/rail. Connector: Test→/v1/metrics + accurate copy; base route docstring/note fixed. Filed follow-up 39 (verdict taxonomy idle/shadow still spend-derived in simulated mode — deeper redesign, out of M3 scope). GATE GREEN: lint ✓0 · tests ✓2881/2881 · build ✓ · tsc ✓ (sequential). Uncommitted (3 milestones now stacked in the WIP tree).
2026-07-05 AUDIT value&market lens done → 🟡 real-but-unproven. Thesis: narrative+self-audit engine is the real, rare moat; but score isn't reproducible (claude-cli has no temp/seed; providers run temp 0.2), GitHub-native bias floors non-GH-native orgs (golang 20 vs 74 same org), CI-gate wedge actually gates the deterministic MOCK not the LLM, and buyer/JTBD unnamed ($10 self-serve vs due-diligence artifact mismatch). Recommends reposition "reproducible score/gate" → "AI-native readiness briefing" + split free deterministic gate / paid narrative.
2026-07-27 SKILL ADOPTED: ship-loop codified from the personas repo → .claude/skills/ship-loop/SKILL.md (state stays in .claude/ship-loop/; gate/dimensions adapted to ascent). Loop invocable as /ship-loop.
2026-07-27 RESUME after 22-day gap (74 commits since M9). PREMISE RECONCILIATION (code-verified): 15 premise wrong (route exists) → cut; 20 mostly done (7 boot files now mapped; residual drift ScanModal/ColdScanGate); 22 provider.ts docstring already fixed; 11/16/17/19/21/26/27/33/35/39 premises still hold.
2026-07-27 GATE RE-CERT (sequential): vitest ✓ 3579/3579 (+636 since M9; 1 contention flake in auth.test.ts when run concurrently with lint — green twice on re-run; methodology: never run lint+vitest concurrently) · build ✓ · tsc ✓ 0 · lint ✗ 13 err/228 warn → 12 err from ESLint scanning stale .claude/worktrees/ copies; 1 real err BadgeGenerator.tsx setState-in-effect. 300-LOC ✗ 3 regrown (PracticeApply 326, RepoSegmentsPanel 317, Simulator 312).
2026-07-27 M11 "re-green the gate" executed: eslint globalIgnores(".claude/worktrees/**") + BadgeGenerator render-time preview reset → lint 0 err, badge tests 4/4, tsc 0. Gate GREEN at 13:40.
2026-07-27 COLLISION — M11 code changes LOST: a concurrent session committed/integrated the tree mid-run (HEAD c3fe9ab→2bb5e90 "report-shell" commits; theirs.tmp + integration-personas-ports worktree = active integration). eslint.config.mjs + BadgeGenerator.tsx reverted to that session's versions; this journal + backlog edits were also wiped (re-appended here). Items 41/42 REOPENED — re-apply after the integration settles. Loop paused at CP2 pending user.
2026-07-27 CP2 RESOLVED (user present): collision over, master handed to the loop. M12 = A invariant repair (44, 20-residual, 19). Starting with a re-measure on the integrated tree.
2026-07-27 M12 start (CP2 user pick = A invariant repair): item 19 ☑ committed 009b514 (tour OnboardingChecklist → TourChecklist, 1 import site, org tests untouched-green). Item 44 re-measured post-merge: 7 files >300 (Simulator 361, LiveWarRoomHeader 352, PracticeApply 336, CreditsControl 329, AlertsControl 324, RepoSegmentsPanel 317, shared/ui 307) — 7 parallel pure-relocation agents dispatched, org/shared trio warned about each other. Item 20 re-scoped: drift is 540/1051 src files unmapped + ghosts (merge brought memory/skills/passports/adoption/live domains) — full map regeneration AFTER extractions land (they create files); Vibeman localhost API unavailable, doing it mechanically.
2026-07-27 M12 item 44 ☑ committed bc77e50 — all 7 agents pure-relocation green: Simulator 361→293 (+ScenarioForm 146), LiveWarRoomHeader 352→295 (+liveWakeLock 63), PracticeApply 336→271 (+BatchResults 56, shared 25), CreditsControl 329→246 (+sections 138), AlertsControl 324→270 (+parts 84), RepoSegmentsPanel 317→264 (+autoAdd 89), shared/ui 307→220 (+uiMeters 93, barrel). PowerShell check: ZERO .tsx >300. Scoped suites green per agent (45+50+6+11+1 etc.); full gate pending after item 20. Transient cross-agent tsc noise again (M7 pattern) — final tree clean per last agents' exit-0 runs.
2026-07-27 M12 item 20: context-map regeneration agent dispatched (fix ghosts, attach 540+ unmapped incl. today's 8 extraction files, new contexts for memory/skills/passports/adoption/live domains, index+revision refresh). Single agent — one-file JSON job.
2026-07-27 M12 item 20 ☑ committed 64a1212 — context-map reconciled (agent, code-verified by my own drift re-run: 0 unmapped / 0 ghosts, JSON valid): 548 files attached, 84 ghost paths rewritten + 9 removed, 4 new contexts (Org Memory 34f, Skills Registry & API Tokens 41f, AI-Native Passports 34f, Portfolio & Leaderboard 8f), index/summary/revision regenerated.
2026-07-27 M12 ☑ COMPLETE — items 19 (009b514), 44 (bc77e50), 20 (64a1212). GATE GREEN (sequential): lint ✓ 0 err (27 warn) · vitest ✓ 4240/4240 (item-45 report-document flake green this run) · build ✓ · tsc ✓ 0 · 300-LOC check ZERO over. → CP3 pending (candidates B/C/D/E in state.md).
2026-07-27 CP3 RESOLVED (user): M13 = B two-tier copy (33,35,17). Copy review gate before commit.
2026-07-27 M13 ☑ COMPLETE (CP3 pick = B two-tier copy; wording USER-APPROVED at review gate) — items 33,35,17 in one commit f7610d4 (surfaces share lines; journaled in lieu of per-item commits). Report kicker → "AI-native readiness briefing" + variance chip (D29); gate PR comment → "Ascent AI-native Scorecard" headline (check-run name pinned for branch protection) + reproducible-by-design mock footer; badge GateSection names the free Scorecard gate + states the deterministic default; pricing sub-headline carries both tier names. Entitlements untouched (34 pending). GATE GREEN: lint ✓0 · vitest ✓4240/4240 · build ✓ · tsc ✓0. Coexistence note: "readiness briefing" (repo) vs "executive briefing" (org) — flagged, user accepted.
