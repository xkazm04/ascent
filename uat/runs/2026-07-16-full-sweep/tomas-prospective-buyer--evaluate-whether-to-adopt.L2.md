
# L2 Report — Tomáš (prospective buyer) × "Evaluate whether to adopt"

cert_level: L2 (empirical, live browser) · Driven against `http://localhost:3000` (already-running dev server, reused). Chrome MCP extension was not connected this session, so driving was done via bespoke Playwright scripts (`uat/driver/drive-tomas.mjs`, `uat/driver/drive-tomas-tabs.mjs`) reusing `uat/driver/drive.mjs`'s conventions. Env pinned per `uat/env.md`: `ASCENT_AUTH_BYPASS=1`, `LLM_PROVIDER=claude-cli`. Scan target: `vercel/next.js` (the journey's own suggested repo). Screenshots/text/ARIA in `uat/runs/2026-07-16-full-sweep/shots/`.

Carried forward from L1 (`l1Priority`): (1) measure actual wall-clock for a live claude-cli scan of a large repo and judge the in-flight copy's honesty, (2) confirm the report surface shows per-dimension evidence + invitational roadmap framing, (3) confirm whether the missing case study is felt as a gap once real scan output is in front of him, (4) spot-check `/about`'s `AboutFeature`/`RoiSimulator` body copy for a who's-it-for statement. Carried-forward major: F1 (scan-latency promise contradicts itself and undersells the real median).

---

## 1. Journal (first-person, in-character, chronological)

**`/` — 0:00.** Same H1 as the code model promised: "Every engineering org has a maturity. Now it has an index," with the 0–100/5-level/9-dimension mechanism named right in the sub-line. No throat-clearing. The register ("9 PUBLIC REPOS RATED... served live from embedded pglite") is a nice touch — it's not a demo screenshot, it says it's live, and I can see real repos I know (react, next.js, deno, prisma) with real-looking per-dimension numbers. I notice `vercel/next.js` is already on the board at avg 82, "2d ago." Filed that away — I'm about to scan it myself, so I'll be able to check their own board against my own result.

**`/about` — quick scroll.** This is a full scrollytelling deck (Overview → Cost → Fleet X-ray → ROI Simulator → Adoption → Risk Radar → Transition → Get started), heavier than I'd normally sit through, but the copy at every section is unmistakably my language: "Stop guessing which teams are ready to accelerate," "Turn 'we think this will help' into 'this moves 6 of 8 repos to L3 by Q3,'" "Avoid the failed audit, the breach, and the 2 a.m. incident." Nobody writes "2 a.m. incident" for a developer audience — that's written for me, the person who gets the call. There's no literal sentence "This is for Directors of Engineering," but I don't need one; every section talks about *my* org, *my* budget, *my* audits. I'd say this clears "who's it for" on vocabulary alone, even without a label.

**`/pricing`.** This is the best page on the site. $0 / $10 / $20 a month, a full capability matrix, and — new since I last heard about pricing tools like this — a 5-scan/month *free* allowance (up from nothing), plus a clear explanit-of-credits box ("Credits buy exactly one thing: a scan beyond your monthly allowance. Cached re-scans... are free"). Only Enterprise hides behind "Learn more" / contact — and Enterprise pricing hiding is normal, I don't need Enterprise's number to evaluate Pro/Team. Genuinely no wall here.

**Scan `vercel/next.js` myself.** I paste it and hit go — no signup, no email gate. First read: "Reading repository metadata" → 28%, ticking through a 6-step checklist (Reading repository metadata, Reading file tree & history, Reading key files, Analyzing 9 dimensions, Asking Claude, Composing your report). The clock (`0:16`, `0:31`...) and the copy underneath ("A live AI scan reads the repo and scores 9 dimensions — this usually takes a few minutes. You can leave this tab open.") both read honest, not evasive. The percentage sits at 72% for what feels like a while once "Asking Claude…" starts (I watched it hold at 72% from 0:16 through 2:47) — that's a genuine stall-*feel*, even though the clock kept moving so I didn't think it was frozen dead. Total: it finished at **3:03 (183 seconds)**. That's real, and it's inside "a few minutes" as promised — better than I expected, and much better than what I'd have guessed from "usually takes a few minutes, could run long."

**The report.** This is the part that decides it for me. Headline: 68/100, L4 Integrated. Below it: a **score waterfall** — every one of the 9 dimensions, its weight, its point contribution, its delta since last scan, summing to 68. Then **Strengths** and **Risks & gaps**, each with specific, real citations: "18,801 test files, 2.90 test-to-source ratio," "36 workflows, required status checks, 1 approval + code-owner review + signed commits... median 0.3h to first review, 7.1h to merge." That's not "add more tests" boilerplate — that's a number I could argue with. Then there's a **"Flagged for review"** box that catches something specific: the sampled `.github/CODEOWNERS` file has only template comments with no real ownership rules, yet the branch-protection signal credits "code-owner review enforced." That is *exactly* the kind of discrepancy a senior engineer flags in a real audit — the tool auditing its own detector, out loud, on my screen. Clicking into **Dimensions** gives me the full radar + per-dimension breakdown with an "Evidence" list and a "Gaps" callout for each (I expanded D1: it names CLAUDE.md, AGENTS.md, the `.claude/` directory, a devcontainer, and specific security rules like "SHA-pin third-party actions" — that's reading the actual repo, not guessing). Clicking into **Roadmap** gives me a "Gaps to explore" list framed exactly as advertised — "Little scans what AI ships for vulnerabilities or secrets," impact/effort tags, a "Fastest path" callout, and EXPLORE bullets phrased as questions ("If an agent pulled in a vulnerable dependency... what would catch it before release?") not orders. This is the "explore, don't dictate" tone the code promised, and it's live on the page.

**Cross-check against the landing register.** Here's where I got suspicious. The landing page's register showed `vercel/next.js` at Agentic=92 two days ago. My own fresh scan just now shows Agentic Workflows=**15**. That's not noise — that's a 77-point swing on the exact dimension the report itself flags as the weakest ("Agentic Workflows scored 15/100... AI isn't in the loop yet"). If I hadn't run my own scan, the front page would have told me next.js's agentic workflows were nearly maxed out; my own scan says they're nearly absent. One of those is wrong, or they're not really the same thing, and nothing on the page tells me which.

## 2. Findings

### F1 (carried forward, re-scored at L2) — Scan-latency promise: partially vindicated, partially still real
- evidence: measured wall-clock **183s (3:03)** for a fresh `vercel/next.js` scan, screenshots `uat/runs/2026-07-16-full-sweep/shots/progress-62s.png`, `progress-123s.png`, `05-scan-final.png`; in-flight copy at `src/components/report/scanEstimate.ts:59` ("...this usually takes a few minutes...") vs. cold-permalink copy at `src/components/report/ColdScanGate.tsx:33` ("...takes about a minute").
- L2 refines L1's structural read in two ways:
  1. **Reachability is narrower than L1 assumed.** `ColdScanGate` (the "about a minute" copy) only renders when a specific `owner/repo` permalink has **no pinned scan for the current commit** (`src/app/report/[owner]/[repo]/page.tsx:111`, `if (!pinned) return <ColdScanGate .../>`). Tomás's actual primary path — paste into `ScanForm` → `/report?repo=...` — never routes through `ColdScanGate` at all; that component only fires if he separately clicks into a register/leaderboard row for a repo+commit not yet scanned. So the two contradicting copies are not both on his primary path — code-checked `present-but-missed` at L1 (the contradiction is real in the codebase, but L1 over-stated how often *this specific buyer* encounters both halves of it in one sitting).
  2. **The copy he does see was honest in this run.** "A few minutes" matched a 183s result — it undersold slightly (183s reads more like "three minutes" than the vaguer "a few"), but it was not evasive, did not silently exceed its own "several minutes" escalation threshold (`SCAN_ESTIMATE_LONG_MS` = 600s, not hit), and the percentage-plus-clock combination never looked stalled even though the percent number itself sat at 72% for ~2:30 of the ~3:03 run (confirmed via `scan.ts:312-317`, a single fixed `pct: 72` emitted at the start of the "score" stage, blended via `Math.max` with the time-based creep in `ReportClientStatus.tsx:101` — by design, not a bug; the clock continuing to visibly tick is what keeps it from reading as frozen).
- **New nuance L2 adds that L1 could not:** the actual measured time (183s) beat even the fastest of the six calibration samples cited in the code comment (272–486s, `scanEstimate.ts:7-9`). This is one live sample, not a re-measurement of the median — I flag but do not claim the ~6min median is wrong; a buyer on a slower day/larger repo could still see 6–10 minutes. Net: F1 downgrades from **major** to **minor** for *this specific buyer's actual primary path* (paste-and-scan), because the copy he actually sees is accurate and the live result cleared his stated patience bar closely enough that the strong report on the other end earns forgiveness (matches his own stated conditional: "if the report... is genuinely senior-grade... I'll forgive the wait"). The underlying cross-copy contradiction (`ColdScanGate` vs `scanEstimate.ts`) remains real and should still be fixed — it's just not the blocker L1's theoretical read suggested for *this* journey.
- code_check: `present-but-missed` (contradiction real, reachability narrower than modeled)
- verdict: `confirmed` (the contradiction exists in code) but severity re-scored `minor` for this journey's actual reachable path.
- suggested_acceptance: align `ColdScanGate.tsx:33`'s "about a minute" with the honest "a few minutes" framing used everywhere else, so the two surfaces agree regardless of which one a given buyer hits.

### F2 (carried forward) — No case study; L2 confirms the live scan substitutes
- evidence: `05-scan-final.png`, `07-dimensions-tab.png`, `08-dimension-expanded.png`, `09-roadmap-tab.png`; the CODEOWNERS discrepancy callout on-screen.
- L2 verdict: **confirmed the OR clause is satisfied.** The live scan output is specific, cites real evidence (18,801 test files, 36 workflows, review-cycle timing), and catches a genuinely defensible discrepancy (CODEOWNERS template vs. enforced branch-protection claim) — the kind of catch Tomás says he'd expect from a staff engineer. He would not feel the absence of a case study as a gap after seeing this; the scan itself is the proof. Severity stays **minor** (matches L1), not elevated.
- code_check: `confirmed-absent` (still no case-study component anywhere in the tree) but `by-design` in effect — the self-serve scan is deliberately the substitute proof mechanism, and it works.

### F3 (carried forward) — Report surface renders per-dimension evidence + invitational roadmap: confirmed present and strong
- evidence: `08-dimension-expanded.png` (Evidence bullets: "Found CLAUDE.md," "Found AGENTS.md," "Found `.claude/` directory," "Found devcontainer," "Defines explicit constraints/rules"; Gaps callout: "No MCP server configuration or custom slash commands/skills visible..."); `09-roadmap-tab.png` ("Gaps to explore — your next rung: L5 Autonomous," impact/effort tags, "EXPLORE" bullets phrased as open questions, a "Fastest path" callout).
- Much of this evidence is even visible on the **default "Scoring" tab** without clicking into Dimensions — Strengths/Risks/Flagged-for-review render inline. A skimming buyer sees citations without extra navigation.
- code_check: `present-but-missed` at L1 (L1 correctly predicted this from the prompt's grounding but could not certify the render); L2 confirms it is not just fed to the model — it's surfaced to the reader, legibly, unprompted.
- No defect. This is a strength to protect: do not simplify the evidence/gaps/discrepancy rendering away.

### F4 (new at L2 — surface-model gap L1 could not see) — Landing register can disagree sharply with a buyer's own fresh scan of the same repo
- evidence: `01-landing.text.txt` (vercel/next.js row: AI Tooling 72, Testing 93, CI/CD 78, **Agentic 92**, AI Process 87, avg 82, "as of 2d ago") vs. `05-scan-final.text.txt` / `07-dimensions-tab.png` (same repo, minutes later: D1=66, D2=99, D3=83, **D4 Agentic Workflows=15**, D8=90, overall=68). The Agentic dimension — the exact one the fresh report calls out as the repo's weakest point ("AI isn't in the loop yet — it's at most at the keyboard," Roadmap tab) — reads 92 on the page he lands on and 15 on the scan he trusts most.
- code_check: the register is genuinely DB-backed (`src/lib/db/scans-read.ts:540-563`, `galleryCardFrom` reads a real persisted `scans[0]`), and the landing route explicitly caches it (`unstable_cache`, `src/lib/db/scans-read.ts:566-572`, "a short shared TTL turns N homepage renders into one DB pass per window"). So this is most plausibly a **stale cached snapshot** of an older, different persisted scan for the same repo (the report page itself showed "2 scans tracked," "no change since last scan" for its own overall number, but the register's raw dimension breakdown did not reconcile with either) — `by-design` caching behavior with an unintended trust side-effect, not a scoring bug per se. I could not fully trace whether the "2d ago" register row and the older of my report's "2 scans" are the literal same row within this pass — flagged as uncertain on root cause, but the **observable discrepancy itself is directly screenshotted and reproducible**.
- verdict: `confirmed` (the discrepancy is real and visible to any buyer who compares the two surfaces) but root cause `uncertain` (stale cache vs. genuine scoring drift between runs — most likely the former).
- impact: `{ frequency: low-med (only a buyer who cross-references, but Tomás explicitly is that buyer — his whole method is "does it reconcile with what I know"), reachability: high (both surfaces are on his natural path — landing register is literally above the fold), trust_erosion: high (a 77-point swing on the specific dimension the report calls "weakest" is not a rounding difference; it reads as "which number do I believe") }`
- l2_priority (for recertify): confirm whether the register cache invalidates promptly after a fresh scan, and whether the register's displayed dimension breakdown is ever this far out of step with a same-day fresh scan under normal (non-back-to-back-testing) conditions.
- suggested_acceptance: either shorten/invalidate the register cache on fresh scan completion for the scanned repo, or label register rows more clearly as a snapshot ("as of 2d ago" is present but easy to skim past next to bold numbers) so a buyer doesn't read it as current-truth.

### F5 (new at L2, minor/polish) — Progress percentage visually holds at a fixed value for ~2.5 minutes of a ~3-minute scan
- evidence: `progress-62s.png` (1:02, 72%) through the log at t=168s (2:47, still 72%); confirmed by design via `scan.ts:312-317` (fixed `pct: 72` on entering the "score" stage) blended by `Math.max` with a slow asymptotic time-based creep (`scanEstimate.ts:33-37`, crossover math puts the time-based curve overtaking 72% only after ~170s).
- This is **by-design**, not a bug — the code comment explicitly says the intent is to keep the bar "moving" via the time-based function — but in this actual run, for a repo whose real scan finished in ~183s, the crossover point (~170s) left only ~13 seconds where the numeric percentage itself visibly changed; the clock and stage label are what carried the "still working" signal, not the percentage. On a scan closer to the ~360s median this would look better (more runway for the time-based creep to visibly advance before completion). Purely cosmetic risk on fast scans; not a trust-breaker on its own (the clock+stage label combo is doing the real work), but worth naming as the honest limit of the current progress-bar design.
- code_check: `by-design`.

---

## 3. Adversarial verification pass

- **F1**: Could a skeptic say "you're cherry-picking one fast sample to excuse a real major"? Fair challenge — I explicitly did not claim the median is wrong, only reported the one live number and reasoned from it plus the code's own documented range. Kept as `confirmed`/`minor` for *this specific buyer's primary path*, with the underlying code contradiction still open as a smaller fix.
- **F2/F3**: Could a skeptic say the report only looks good because `vercel/next.js` is a famous, well-instrumented repo (an easy target)? Plausible, but that's exactly the repo class Tomás's own character file and this journey specify he'd test with ("a repo he already understands... vercel/next.js or facebook/react") — testing on his chosen repo class is the correct test, not a flaw in the finding.
- **F4**: Could a skeptic say this is just normal eventual-consistency and no real buyer would ever compare that closely? Tomás's stated method is explicitly "does the score reconcile with what I already know about that codebase" — a buyer who runs his own scan specifically to check the tool's credibility is very likely to glance at the number the landing page already showed him for the same repo he just chose. Kept as `confirmed`, `uncertain` root cause, impact `high` for this buyer specifically.
- **F5**: Could a skeptic say a 72%-then-jump is standard async UX and nobody notices? Mostly agree — downgraded to `minor`/cosmetic, kept only because it's a real, screenshotted gap between the code's stated intent ("must keep moving") and what this run actually showed.

---

## 4. Character voice reaction (first-person, post-live)

"Okay. I said I'd give them the front door, and after actually clicking through it, I'm giving them more than that — the pricing page is as clean as anything I've evaluated this year, and the about page, even though it's a longer scroll than I'd like, never once made me guess who it's for.

The scan is the real story. Three minutes, not the six I was bracing for from the docs I half-read, and what came back is not a demo. A test-to-source ratio, a review-cycle time, and — this is the part that got me — it caught that the CODEOWNERS file in this actual repo is a template with nothing in it, while the branch-protection rule takes credit for 'code-owner review enforced.' I've seen a senior engineer make exactly that catch in an audit. That's not a toy. That's the report I'd forward.

But then I did the thing I always do — I checked it against what the front page told me two minutes earlier. Same repo. Agentic score: 92 on the way in, 15 on the way out. Ninety-two isn't 'close to' fifteen. If that's a stale cache and the tool's real answer is 15, fine, tell me that on the front page instead of showing me a number I'm going to remember and then contradict five minutes later. Right now the front door is selling something the back room walks back.

Verdict: worth a deeper look — a real yes, not a conditional one, on the strength of what my own scan gave me. But before I put my name on this in front of my VP, somebody needs to explain why their own homepage doesn't agree with their own scanner about the same repo. That's not a dealbreaker, it's a 'fix this before I demo it to my boss' — and I'd tell a peer exactly that: 'the scan is the real thing, go run it yourself — just don't trust the number on the homepage until you've run your own.'"

Would he adopt: **leaning yes** — a step up from L1's "leaning yes, conditionally" because the live scan cleared the senior-quality bar decisively and the latency fear was largely unfounded on his actual path. The register/scan mismatch (F4) is the one thing that would make him loop in a colleague to double-check before recommending it upward, rather than recommending it outright today.

---

## 5. What passed (strengths worth protecting)

- Numeric, wall-free pricing (`/pricing`) — exactly the G2/pricing-transparency bar, with more self-serve detail (a full capability matrix) than the bar strictly required.
- Frictionless CTA — paste-a-repo, no signup, straight into a real scan.
- The report itself: score waterfall with full attribution, per-dimension Evidence/Gaps, a self-auditing "Flagged for review" discrepancy catch, and an invitational (not prescriptive) Roadmap tab with impact/effort tags.
- Honest in-flight scan copy on the primary path ("a few minutes... you can leave this tab open") that matched the actual ~3-minute result in this live run.
