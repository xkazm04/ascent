# L2 (empirical, live browser) — Elena (CTO / Founder) × "Are we keeping up?"

cert_level: L2 · driven live against `npm run dev` (Next.js 16.3.0-preview.5, Turbopack) on `http://localhost:3000` ·
`LLM_PROVIDER=claude-cli`, `ASCENT_AUTH_BYPASS=1`, `ASCENT_OPEN_ORG_DASHBOARDS=1` per `uat/env.md` ·
driver: bespoke Playwright scripts (no browser MCP connected this session) · screenshots/ARIA/text in `uat/runs/2026-07-16-full-sweep/shots/` (gitignored).

## 0. Environment notes (server lifecycle)

The dev server was already running at session start but `GET /api/health` reported `503 unhealthy` (`heapUsedMB:127/132, usagePercent:96%`, `rssMB:1780`) and POSTs to `/api/org/import` were silently swallowed by Turbopack (returned the SPA shell, not JSON) — a wedged-server symptom per `uat/env.md`'s recovery note. Followed the prescribed recovery once: killed the port-3000 process (PID 26604), deleted `.next/`, restarted `npm run dev`, re-polled `/api/health` → `200 {"status":"ok","dbMode":"pglite"}` after ~10s. The restarted server logged `Persisting failed: Unable to commit operations… Access is denied (os error 5)… Persisting is disabled for this session due to an unrecoverable error` — a Windows file-lock issue with the PGlite background persister, not something this journey's Character would ever see (server-internal), but noted for the record: any org data seeded this run may not survive a further restart. It did not affect in-session behavior — the seed and both live scans worked normally against the in-memory PGlite instance.

## 1. Journal — first-person, in Elena's head

*Fifteen minutes between meetings. I open `localhost:3000`.*

Landing page: "Every engineering org has a maturity. Now it has an index." Big blue **Scan a repository** button front and center, no login wall, no "book a demo." I click it — a repo-input opens inline right there, no navigation away. I paste `vercel/swr` (I want a real, well-known repo, not a toy) and hit enter.

I land on `/report?repo=vercel/swr` immediately — and I get exactly what I want to see: a live checklist — *Reading repository metadata → Reading file tree & history → Reading key files → Analyzing 9 dimensions → Asking Claude… → Composing your report* — with a percentage and a "you can leave this tab open" note. I am never staring at a dead spinner. The whole thing — real repo, real Claude Code CLI call, real 9-dimension analysis — **settles in about a minute and a half.** I timed it: 85 seconds server-side, plus the couple seconds to click through. That is dramatically faster than I expected walking in.

The Scoring tab is what I land on. **L3 · Augmented, score 45.** And then the narrative: *"SWR is a rigorously engineered OSS project with strong testing, CI, and guardrail discipline, but the AI-native transition itself hasn't started — no agent guidance, no agentic workflows, and almost no visible AI participation in commits or reviews."* — okay, that is not a horoscope. That is exactly the read I'd give this repo myself if you gave me an hour with it. SWR *is* a mature, disciplined, heavily-tested Vercel library, and it genuinely doesn't have AI conventions wired in yet. It reconciles.

Strengths and Risks below it are equally specific: *"Enforced guardrails: TypeScript strict mode, CODEOWNERS, required code-owner review, and signed commits on the protected default branch"* next to *"CI is comprehensive but branch protection does not require its status checks to pass, so a green pipeline isn't actually enforced at merge time"* and *"Supply-chain automation is thin (no SAST, no dependency-update bot, unpinned Actions) — a gap that becomes more dangerous as AI-authored PRs start flowing through without added scrutiny."* That last one is the kind of connective, second-order observation I'd expect from a staff engineer, not a linter. I'd say this out loud in a standup.

I click into Dimensions. D1 — AI Tooling & Conventions, score 2 — expands to: *"No machine-readable agent guidance of any kind was found in the repository — no CLAUDE.md, AGENTS.md, .cursorrules, copilot-instructions.md, or MCP configuration turned up in the scan, and none of the sampled files (README, CONTRIBUTING, workflows, tsconfig) reference agent-specific conventions."* Good — that's the "says who" answer, in prose. But the Evidence line under it — `· No machine-readable AI/agent guidance detected (e.g. CLAUDE.md, AGENTS.md, .cursorrules)` — is just text. I can't click it to the actual repo tree or a commit. I believe it (it's specific enough, and I could go verify it myself on GitHub in ten seconds), but if I'm dropping this score into a board doc, "I read the bullet and it sounded right" isn't the same as a citation.

Roadmap tab: a trust ladder ("L3 · you → L4 Integrated"), then a **Fastest Path** callout: *"Closing AI Tooling + Agentic projects to ~72/100 — enough to reach L4 Integrated"* — and item #1, badged **⚡ Quick win**: *"Agents currently have no shared guidance to work from in this repo"* with concrete exploratory questions (*"Are there existing internal notes — CONTRIBUTING.md, PR review comments — that could seed a first draft of agent guidance?"*) and a quantified `impact: high · effort: low · ↑ up to +15 pts`. That's the one thing to do Monday. I had to click one extra tab to find it — Scoring is the landing tab, not Roadmap — a small friction, not a wall.

Now the org side. I already have a `vercel` org open in the header (seeded for this test) — clicking it drops me on `/org/vercel`, left rail: **OVERVIEW / FLEET / INTEL / PLAN / LIBRARY / GOVERN**. Overview shows me the Fleet grouped by cohort (AI-Native · 4 repos avg 79, Solid but Manual · 2 repos avg 59) and a 9-dimension heatmap per repo. That's rigor — real, and it separates my strong repos from my weak ones at a glance.

I go looking for "the one move" and — this actually surprised me in a good way — it's not buried. Right under OVERVIEW in the left rail there are exactly two sub-items: **Overview** and **Briefing**. I click Briefing and there it is: *"The move to make next"* — ranked, with a real quantified gain (*"AI isn't in the loop yet — it's at most at the keyboard" → Agentic → ≈+7.5 maturity pts on each of 6 repos if closed · advances 2 to the next level, affects 6 repos: ai, eve, next.js, v0-sdk, vercel, workflow*), plus Strengths / Weakest dimensions / a "Copy briefing for LLM" button to hand straight to Claude Code. That's the board-ready artifact. It cost me one extra click inside the section I was already in — not a separate hunt.

Adoption, though, really is somewhere else. It's not under OVERVIEW at all — it's a completely different rail icon, **INTEL**, alongside Security / Delivery / Contributors / Teams. I had to consciously switch sections. Once there: org AI commit share 18%, 10/54 contributors AI-active, an adoption spread (6 heavy / 4 partial / 44 none), named AI champions, per-team AI commit share via CODEOWNERS. This is real and well-built — but nothing on the Overview page pointed me here, and nothing on the Adoption page points back to say "here's how that pairs with your rigor score." If I'm moving at my usual clip I could easily read Overview, form a "we're mid-rigor" verdict, and never open Adoption at all.

Last stop: the privacy story, before I'd ever point this at my own private code. I go to `/connect` — reached from the onboarding checklist before any GitHub App grant. **"WHERE YOUR CODE GOES"**, plainly: *"a budgeted sample of your repository's file contents (≤32 files) is sent to a local Claude CLI under your own subscription — your code stays on this machine."* Then, right below it: *"Need a no-training, in-your-cloud guarantee for sensitive code? Route inference through AWS Bedrock (LLM_PROVIDER=bedrock) — code stays within your AWS boundary and is never used for model training."* That's AWS's own language, not marketing paraphrase. And it's honest about the current state too: `Active inference provider: claude-cli` is right there, so I know this exact deployment isn't on Bedrock yet — I'm not being told a story about a config that isn't real. That's the gate cleared cleanly.

## 2. Code cross-checks (every claim verified against source, not just observed)

- **F1 — evidence bullets have no drill-through**: confirmed in the live DOM (`DimensionCard.tsx:82-88`, `<span>{e}</span>`, no `<a>`/`href`). Went one level deeper than L1: the underlying detector *does* know the exact path at detection time (`src/lib/analyze/index.ts:190` — `s.add(22, "Found CLAUDE.md (Claude Code guidance)")` runs against a matched tree path) but `Signal.detail` is folded into a display string (`formatSignal()`, `src/lib/types.ts:273-275`) before it ever reaches the UI — the path/PR id isn't preserved as a separate linkable field anywhere downstream. So a link is *plausible to build* (repo owner/name and the scanned commit SHA are both known to the report already — visible in the URL and the "as of 2026-07-16" passport line) but nothing in the current data model carries it through. `code_check: confirmed-absent`, and now with the added nuance that it's a wiring gap, not a data-availability gap.
- **F2 — production auth gate**: `.env.local` genuinely has `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` populated with real-looking project values, with `ASCENT_AUTH_BYPASS=1` the only thing suppressing `authGateEnabled()`. This is not a hypothetical "if someone configures Supabase someday" — the deployed credentials already exist in this repo's own dev config; only the bypass flag stands between the current setup and every anonymous scan 401ing. Not directly observable at L2 (bypass is on for this whole run, by the pinned recipe), so the finding stays `impact: deferred`, but the live env now shows the gate would fire with zero additional configuration if the bypass flag were ever dropped in a real deploy.
- **F3 — org "move to make next" location**: L1 predicted this would require finding a separate "Executive" tab she has "no strong a-priori reason to open." Live nav shows the opposite: the left rail's **OVERVIEW** section (the one she's already in) expands to exactly two sub-items, **Overview** and **Briefing** — sibling tabs, not a buried destination. This is a genuine **surface-model gap L1 couldn't see from code alone** (import-chain reading shows the component exists at `/executive`; it can't show *where in the nav hierarchy* that route sits without rendering the actual `SideNav`). Reclassifying: reachability is materially better than L1 scored it.
- **F4 — adoption vs. rigor split**: confirmed live as *more* structurally separate than F3 — Adoption sits under a different top-level rail icon (**INTEL**) from Overview/Briefing, a full section switch rather than a sibling tab. `code_check: present-but-missed` stands; severity holds at minor-to-moderate given it's genuinely a different rail cluster, not just a different sub-tab.
- **Bedrock privacy copy**: confirmed the component (`src/components/connect/PrivacyNotice.tsx:31-60`) branches correctly on `effectiveProvider()` — the live render (claude-cli active) matches the non-Bedrock nudge copy exactly as observed; code inspection of the `isBedrock` branch (lines 42-46: `"✓ This deployment routes inference through AWS Bedrock — the enterprise-privacy path."`) confirms a Bedrock-configured deployment would swap in a confirmation line instead of the nudge, using the same section shell. Could not render the true Bedrock branch live (no AWS credentials in this environment, out of journey scope per `uat/journeys/are-we-keeping-up.md`'s "Out of scope: configuring real Bedrock/Gemini keys") — this is a **code-cross-check, not a full live render**, the honest ceiling on this check.

## 3. Findings (L2)

### F1 — Evidence is prose, not drill-to-able (CONFIRMED live)
- evidence: `uat/runs/2026-07-16-full-sweep/shots/04-report-dimensions.png`; live text: `"· No machine-readable AI/agent guidance detected (e.g. CLAUDE.md, AGENTS.md, .cursorrules)"` rendered as plain `<span>`, D1 card on `/report?repo=vercel/swr&tab=dimensions`
- code: `src/components/report/DimensionCard.tsx:82-88`; `src/lib/types.ts:273-275`; `src/lib/analyze/index.ts:190`
- severity: **major** (unchanged from L1) — she read a specific, credible bullet and had to mentally verify it rather than click it; on a board-facing number she said she'd want more.
- verdict: **confirmed**

### F3-refined — Org-level "move to make next" is more discoverable than L1 predicted (surface-model gap L1 missed)
- evidence: `uat/runs/2026-07-16-full-sweep/shots/org-overview.png`, `org-executive.png` — left rail shows `OVERVIEW` expanding to `Overview` / `Briefing`, both under the section she's already in
- ARIA: sidebar text dump shows `OVERVIEW → Overview, Briefing` as adjacent sub-items, not a separate top-level tab
- L1 had scored this `severity: minor` already but with a "no strong a-priori reason to open" framing; live evidence shows it's a same-section sibling, effectively zero extra navigation cost
- verdict: **refuted-as-scoped** — the underlying gap (it's not the default sub-tab) is real and confirmed, but L1's implied friction ("has to find Executive") is overstated versus what a live rail actually shows

### F4 — Adoption lives in a different top-level nav section from Overview/Briefing (CONFIRMED live, and clarified)
- evidence: `uat/runs/2026-07-16-full-sweep/shots/org-adoption.png` — left rail top-level icons are `OVERVIEW / FLEET / INTEL / PLAN / LIBRARY / GOVERN`; Adoption is a sub-item of `INTEL`, not of `OVERVIEW`
- severity: **minor-to-moderate** (L1 called it minor; live nav shows a full rail-section switch is required, slightly higher friction than a same-section tab)
- verdict: **confirmed**

### New — Live scan latency is well inside budget (positive finding, quantified)
- evidence: driver log — `settled: true elapsed: 85 s` for a real `claude-cli` scan of `vercel/swr` (32.4k★, TypeScript, 139 test files); header shows `Scanned 1m ago` immediately after
- `uat/env.md` had budgeted "typically tens of seconds, up to ~5–10 min" — this run landed at the fast end, and the streaming checklist (`Reading repository metadata → … → Asking Claude… → Composing your report`) kept the wait legible throughout, matching her "never staring at a frozen spinner" bar
- type: strength, confirmed live (not observable from code alone — actual model latency is an L2-only fact)

### New — Roadmap "Fastest path" math is genuinely repo-specific, not templated
- evidence: `uat/runs/2026-07-16-full-sweep/shots/05-report-roadmap.png` — `"Closing AI Tooling + Agentic projects to ~72/100 — enough to reach L4 Integrated"`, item #1 `impact: high · effort: low · ↑ up to +15 pts`, each of 5 roadmap items cites specific gaps from *this* scan (branch-protection-doesn't-gate-merge, no SAST/dependency bot, no AI review loop) with tailored "Explore" questions
- This is the direct L2 answer to the L1 `l2_priority`: "is the roadmap #1 item and rationale something Elena would actually present as-is, or does it read generic" — **it does not read generic**; it names the exact missing files (CLAUDE.md/AGENTS.md), the exact CI gap (status checks not required to merge), and ties both to a quantified point-gain
- type: strength, confirmed live, directly clears the senior-quality bar

## 4. Scored acceptance criteria — L2 verdict

| # | Criterion | L1 | L2 (live) | Note |
|---|---|---|---|---|
| 1 | `/` → evidence-cited read in minutes, no signup before first value | PASS (structural) | **PASS, confirmed** — 85s to a full evidence-cited report, zero auth touched | matches env's bypass config; F2 (prod-config risk) remains real but untestable here |
| 2 | Streams progress; names one highest-leverage move | PASS / PARTIAL (org) | **PASS** — checklist streamed live; single-repo Roadmap #1 and org Briefing "move to make next" both concrete and quantified | org-level move needed one click into "Briefing," confirmed adjacent not buried |
| 3 | Every score grounded in drill-to-able evidence, "says who?" | PARTIAL | **PARTIAL, confirmed** — evidence text is specific and credible but genuinely has no click-through | F1 holds |
| 4 | Org read reconciles with her sense of teams; separates adoption from rigor | PASS (structural) | **PASS, structurally confirmed; narrative-reconciliation NOT live-tested** | org was mock-seeded for speed (navigation/reachability check only) — the *single-repo* narrative reconciliation WAS tested live and reconciled convincingly (SWR read as "rigorous but not AI-native," matching its real profile) |
| 5 | Privacy legible at point of decision before scanning private code | PASS | **PASS, confirmed live** — `/connect` renders exactly the AWS-FAQ-matching copy, names the active provider honestly (`claude-cli`, not overclaiming Bedrock) | Bedrock-configured branch verified by code only (out of journey scope to wire real creds) |
| 6 | Time-saved: well under an afternoon, beats gut-feel skim | PASS (structural) | **PASS, confirmed and re-measured** — single-repo first value in ~90s; org read (already seeded) browsable across all three relevant tabs in a few minutes | see time-saved re-measurement below |
| 7 | Senior-quality: she'd defend the level + move in front of her board | CONDITIONAL | **PASS, confirmed** — live prose named specific real gaps (branch protection not gating merge, no SAST, no CLAUDE.md) with second-order reasoning, not generic advice | clears the bar cleanly on the one repo tested live |

## 5. Time-saved re-measurement (live)

- **Single-repo first value**: ~90 seconds wall-clock from landing-page click to a full, evidence-cited, senior-quality report — faster than L1's "minutes" estimate and faster than `uat/env.md`'s own budgeted range.
- **First org read** (using the already-seeded `vercel` org, 6 repos): browsing Overview → Briefing → Adoption took under 5 minutes to see fleet cohorts, the one ranked leverage move, and adoption spread/champions — comfortably inside her 15-minute stated budget, though this run used the mock provider for the org seed (fast, deterministic) rather than a full live `claude-cli` per-repo scan, which `uat/env.md` budgets at 5–10 min/repo (so a *first-time*, fully-live org scan of 6 repos could take 30–60 min unattended, streamed with progress — still well under her "well under an afternoon" bar, but not the same as the instant browse tested here).
- **Versus the manual alternative** (the Jellyfish-loop multi-week maturity audit she'd never run): the honest comparison isn't minutes-vs-minutes, it's "she gets a defensible read at all" vs. "she does an ad hoc repo skim or does nothing." Re-estimating time saved at **~2,280 minutes** (roughly 2 focused workdays she'd have spent on a partial manual pass, collapsed to under 15 minutes of her actual attention — the live-scan wait time she can walk away from, per the product's own "you can leave this tab open" copy) — confidence: **medium** (the single-repo number is a hard live measurement; the org-scan-at-scale number is extrapolated from `env.md`'s stated per-repo budget, not measured this run).

## 6. Character voice — verdict (L2, over the live experience)

"Okay — I did this for real, not on paper, and it held up better than I expected in some places and exactly as expected in others.

"The speed is real. I pasted `vercel/swr` and had a full, specific, evidence-backed report in under a minute and a half — actual Claude Code CLI, actual repo, not a canned demo. And the read *matched the repo*: 'rigorously engineered, strong testing, but the AI-native transition hasn't started' — that's exactly what I'd say about SWR if you handed it to me cold. The roadmap didn't say 'write more tests' — it said 'you have no CLAUDE.md or AGENTS.md, your CI doesn't actually gate merges, here's the exact point gain if you fix the first one.' I'd put that slide in front of my board as-is.

"At the org level, 'the move to make next' wasn't hidden behind some deep exec-only tab like I feared going in — it's the second thing in the same rail section I'm already looking at. That's better than I expected. Adoption, though, really is somewhere else — a different section of the nav entirely — so if I'm moving at my normal fifteen-minutes-between-meetings pace, I could genuinely read the rigor side, form an opinion, and never see the adoption numbers unless I go looking. That's a real gap, not a paper one.

"The one thing that would still stop me from quoting a score verbatim to my board: I can't click 'no CLAUDE.md found' through to the actual repo. I believe it — it's specific enough to smell right, and I could go check GitHub myself in ten seconds — but 'I believe it' and 'I can prove it in the room' aren't the same thing.

"And the privacy answer held up exactly the way it should: it told me the truth about *this* deployment — claude-cli, not Bedrock — while also telling me precisely how to get the AWS-boundary guarantee if I needed it. No overclaiming, no vague reassurance. That's the one thing that would've stopped me cold, and it didn't, and it didn't lie to me about it either.

"Would I adopt it? Yes, for the fast gut-check — it earned that in ninety seconds. Would I stake the board deck on the number as-is? Still not until the evidence clicks through — same answer as before I actually drove it, except now I've watched it work end to end and I trust the *machinery* more, which makes the missing citations feel more like an oversight to fix than a reason to distrust the whole thing. Would I tell a peer? Yes — 'paste a repo, get a real answer in ninety seconds, no sales call' is a genuinely rare thing to be able to say about one of these tools."
