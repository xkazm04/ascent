# Backlog

Tracked, evidence-backed work items. Every entry cites its origin: a UAT finding id, a harness scan,
or an issue. **No item enters without a citation**; an idea with no evidence is not a backlog item.

Sections are per-source-run so an item's provenance survives triage. Status vocabulary:
`open` · `in-progress` · `shipped (<sha>)` · `shipped-unverified` · `resolved-verified` (UAT
`recertify` confirmed it live) · `declined (<reason>)`.

> A shipped UAT item is **not done at merge**. It re-enters `/uat recertify`, verified live against
> the originating Character's scored criteria. `shipped (<sha>)` is a waypoint, not a finish line.

---

## UAT run `2026-08-10-ascent-first` (drained 2026-08-10)

Analysis: [`docs/product/uat-insights/2026-08-10-ascent-first.md`](./product/uat-insights/2026-08-10-ascent-first.md).
Characters: Sam (staff engineer), Dana (VP engineering), Tomáš (prospective buyer).
Drain tally: **15 build · 4 concept-doc · 4 decline**.

**Guardrails binding every item in this section** (from the run's strengths; full text in the
analysis doc §"Strengths → do-not-touch guardrails"):

- G1: no report/PDF cleanup may remove or soften **LLM-vs-detector disagreement**.
- G2: no templating pass may flatten a **counted evidence line** into a catalogue item.
- G3: the **adoption/rigor structural split** and its cohort-matched deltas are not to be merged or
  re-scoped.
- G4: the ranked "one move" must keep **refusing to emit numbers the data can't support**; new
  basis text degrades to absence, never to a fabricated basis.
- G5: no response to "the LLM only moved the score 2 points" may **widen the guardband** or give the
  model more latitude. The discrepancy budget stays all-or-nothing; D9 stays deterministic.
- G6: D2's **assertion-substance sample** and its `detail` string survive any evidence rework.
- G7: the **honest scan-progress component** is not to be touched when fixing the dialog's ETA copy.
- G8: pricing stays **numeric, anonymous, one click**; no "talk to sales" on Pro/Team, no auth in
  front of pricing.
- G9: the **engine-mix caveat stays in the PDF body**, not a footnote.
- M1 (method): **any change to the briefing PDF re-runs Dana's journey before merge.** Standing
  commitment in place of the human read-aloud pass she asked for.

### Build

| # | Item | Evidence | Size | Status |
|---|---|---|---|---|
| B1 | **`/usage` credit alarm must derive from the same authority as the 402.** The banner is non-monotonic (0 credits + 0 scans → "will be refused"; 1 credit + 0 scans → silence) and fires as the **default state of every new org** (`scanCredits DEFAULT 0`), while `AllotmentPanel` says "comfortably within your allotment" eight lines below. Route it through `checkScanEntitlement`/`resolveScanCharge`. | `DANA-L1-003` (**recurrence 2**, confirmed live) · `src/app/usage/page.tsx:142` · `usageDashboard.tsx:46-52` · `AllotmentPanel.tsx:59-64` | S | **code-verified 2026-08-30** (`RV-B1-credit-banner`, resolution `fixed`; two walkers independently) — live confirm still owed on a fresh Free org, cheapest fixture in the run, pairs with `MC-B21` |
| B2 | **Open the advertised free, no-signup public scan.** Scope the sign-in wall to private/org scans; anonymous single-repo public scans proceed under the existing rate limit + public quota. Everything read-only is already open; the one walled action is the only one that converts a buyer. | `TOMAS-L1-01` (**blocker**, L2-confirmed 401) · `src/lib/scan-gates.ts:77-83` · `src/app/api/scan/route.ts:258-261` · `src/app/api/scan/stream/route.ts:79-92` · `README.md:94-98` | S | **server half resolved-verified 2026-08-30** (`RV-B2-public-scan-endpoint`; cookie-less `POST /api/scan` → 200 in 3.3 s, arm A). **The client half was never changed and is this run's blocker → `MC-B2`.** |
| B3 | **Board PDF: stop printing a regression under "Value this period"; label the four repository denominators; don't caption an absent percentile.** Sign-aware value line, scoped denominator wording, benchmark tile that says "not enough peers" instead of "vs 1 repos". Honors G1/G3/G9: nothing is suppressed, only labelled. | `DANA-L1-010`, `DANA-L1-012`, `DANA-L1-011` · `src/lib/org/briefing.ts:54-61,280,295-305` · `src/lib/pdf/briefing-document.tsx:114-140` · `ExecutiveTab.tsx:123-128` | S/M | **resolved-verified 2026-08-30** (`RV-B3-briefing-denominators`, read off the board PDF and the Copy-for-LLM markdown in arm B; M1 worked — the journey was re-run). **Closed.** Ceiling: the same artifact still fails on trajectory honesty → `MC-B1` |
| B4 | **Populate `Signal.detail` across all detectors** so every dimension score cites re-traceable evidence (paths/counts), not a label. Sam's automatic-trust-failure clause and the entire ~30 min gap between his possible and realized time-saved. Honors G6. **2026-08-28:** `RepoIndex.first()` recovers the path a presence check matched and then discarded; D1 (all presence + quality signals), D2 framework/e2e/coverage, D5 documents, D6 type-check/pre-commit/CODEOWNERS and D9 SAST/SCA/SECURITY.md/threat-model now cite it. A signal fired by the manifest/workflow TEXT blob stays unsourced on purpose — there is no file to name — so the remainder (D3/D8 and the text-only D9 checks) is not closable this way and needs a different evidence carrier. `evidence-source.test.ts` pins both directions. | `SAM-L1-01` | L | **partly shipped** — path-triggered half `fixed` (`RV-B4-signal-paths`, code-verified; arm A's absence-shaped subject could not exhibit it live). Residue narrowed and downgraded major → minor by L2, and its owner moved: the LLM prose already cites the paths, so the detectors lag the model → `MC-B7` |
| B5 | **Permalink affordance on the report.** The scan flow ends on `/report?repo=…` without ever surfacing the durable permalink. (The badge half of this item is **dropped**: the README badge feature was removed on 2026-08-29.) | `SAM-L1-04` (`SAM-L1-03` closed by removal — `RV-SAM-L1-03-badge`) | S | **closed 2026-08-31** by `MC-B4` (`9d85f01a`, recertify pass 2) — the report header hands over three separately-copyable payloads and the README job is served as a markdown link. Ceiling: still no image/SVG endpoint |
| B6 | **Render `scoreIntegrity`.** ~~computed, typed and persisted~~ — it was computed and typed and reached **no column at all**; the row said otherwise. Now persisted (`Scan.scoreIntegrityJson`) and shown: the report header carries an integrity chip (`ScoreIntegrityChip`) and the loop cockpit's outcome ledger chips the same record, both via one `integrityNotes` so they cannot word it differently. **Remaining half:** `ProvenanceTrack` (`DimensionCard.tsx:120`) still draws a fixed ±`LLM_GUARDBAND` zone on a dimension whose band was doubled, and still hides the blend weight — the data to fix it is now readable. Honors G5 (surface the real band; do not widen it). | `SAM-L1-02` | M | **chip half resolved-verified 2026-08-30** (`RV-B6-integrity-chip`); the defect it created is **closed 2026-08-31** by `MC-B3` (`904bcc21`, recertify pass 1) — the doubled band is drawn at 56.64 px and the blend weight renders. **The fix created one more**: chip and track now print the same fact in two units → `MC-B35` |
| B7 | **Label facts the model never received.** `techStack` (with `TECH_STACK_PROMPT` off), `contributors`, `aiChanges` render beside model-produced scores with nothing distinguishing detected-and-sent from detected-and-displayed. Include the confidence chip's mislabel (it measures *fetch* coverage, not *prompt* coverage). | `L2-NEW-02` · `SAM-L1-07` (label half only; the cap itself is declined, D2 below) | S | open |
| B8 | **Reconcile the public-scan promise with the 5/mo quota.** "Unlimited free public scans" is contradicted by `public-scan-quota.ts`, and the pricing page and FAQ JSON-LD disagree with each other. Ships with B2 as the honest half of the same promise. Honors G8. | `TOMAS-L1-02` · `src/lib/plans.ts:54` · `src/lib/public-scan-quota.ts:53-60` · `src/app/pricing/page.tsx:100-101` · `src/app/page.tsx:56` | S | **closed 2026-08-31** by `MC-B5` — one derived allowance across the card, `/pricing`, the FAQ JSON-LD and the 429; the 429 now names the Starter label. |
| B9 | **A basis clause on every trajectory/ETA**, on the same line, e.g. *"L4 by mid-October, fit over 9 scan days across 84 days."* The low-data fix currently **nulls** `forecastConfidence`, omitting the hedge rather than replacing it. Honors G4. Blocked from `resolved-verified` by the fixture gap (M-item below), not from shipping. | `DANA-L1-001` (**recurrence 2**), `DANA-L1-002`, both `uncertain — not reproducible on this host` · `src/lib/org/briefing.ts:283-287` | S | **closed 2026-08-31** by `MC-B1` (`4aa12080`, recertify pass 1) — the clause now refuses on low data and states its basis on the card, the markdown and the decoded PDF. Ceiling: the `compacted` tail has still never rendered |
| B10 | Scan dialog says "about a minute"; the app's own calibration says 100–330 s. Fix the dialog copy only, per **G7**. | `TOMAS-L1-03` | XS | **resolved-verified 2026-08-30** (`RV-B10-scan-duration`; 155.4 s measured, no early client timeout). **Closed.** Ceiling: the honest sentence lives in the dialog `MC-B2` replaces |
| B11 | Enterprise, the only tier that fits a 150–250 engineer org, has no reachable contact path; its button goes to `/about`. Add the path; **G8** forbids touching the Pro/Team cards. | `TOMAS-L1-06` | XS | **resolved-verified 2026-08-30** (`RV-TOMAS-L1-06-enquiry-dialog`, presence + affordance type; the POST round-trip deliberately not exercised). **Closed.** |
| B12 | `/about`'s ROI simulator is eight fabricated numbers. Label it as illustrative or remove it. | `TOMAS-L1-04` | XS | **closed 2026-08-31** by `MC-B6` — rendered illustrative label (labelled, not removed). |
| B13 | A configured-but-empty DB renders a zero-row ranking table under a real heading. Hide the section or seed a curated set (the neighbouring `topAiNative`-empty branch already does this correctly, so copy it). | `TOMAS-L1-05` | XS | **closed 2026-08-31** by `MC-B8d` (`0d088f41`, recertify pass 2) — a genuinely empty database renders no repos-rated counter at all. Adjacent: the *worded* empty state cannot fire either → `MC-B39` |
| B14 | Add a per-item **"first step"** field to roadmap recommendations so the concrete move isn't buried in the invitational rationale paragraph. Additive only, per **G2**. | `SAM-L1-05` | S | **fixed 2026-08-31, pending live verification** — `firstStep` shipped end to end (r14) and the first live r14 reading returned it on **0 of 9** items (`RC2-N1`); `ece52101` (r15) makes the ROADMAP COVERAGE block ask for it. One fresh scan closes it → `MC-B8a` |
| B15 | Small correctness batch: "Flagged for review" never says what each auditor claim *did* (`SAM-L1-06`); `scoreLabel` covers 4 of 6 providers (`SAM-L1-08`); the landing deck has no pricing section despite a header comment claiming it does (`TOMAS-L1-07`); `resolveTextRunnerForOrg` (`llm/text-org.ts:28`) is a dead seam with no production caller. | `SAM-L1-06`, `SAM-L1-08`, `TOMAS-L1-07` | XS | `SAM-L1-06` and `SAM-L1-08` **closed 2026-08-31** by `MC-B8b`/`MC-B8c` (`0d088f41`, `97d48243`, recertify pass 2) — each flagged row ends in `widened` with its clause, and `SCORE_STEP_LABEL` is a total `Record` over `ProviderName` including `local` (that branch compile-verified only). `TOMAS-L1-07` and the `resolveTextRunnerForOrg` dead seam remain open |

### Concept-doc (design questions remain, so write, don't code)

| # | Item | Evidence | Status |
|---|---|---|---|
| C1 | **What the model actually contributes.** Measured: the LLM used ≤24% of its ±25 guardband, moved the headline ~±2 points, and returned two dimensions byte-identical to the detector, while producing the roadmap and the discrepancies block. The product leads with the ring and gives away the reasoning; the README's "calibrates the signal scores" is the weakest true claim available. Extend `docs/features/scanning/maturity-model.md`. **G5 forbids the "give the model more latitude" answer.** | `L2-NEW-01` · `_L2-control-arm-llm-vs-signal.md` | open |
| C2 | **Give the model a comparison class and a history.** Three Characters independently named the same absences: prior-scan history, peer/industry cohort context in the FACTS payload, and a full file-tree manifest. All change the **grounding denominator** (a scored instrument of the UAT method), so the trade-off gets written before it gets built, then a journey to certify it. | `SAM` + `DANA` + `TOMAS` near-findings (convergence, no finding id) | open |
| C3 | **Briefing-narrative egress.** The narrative bypasses `src/lib/llm/` and POSTs the fleet briefing straight out; the seam that would fix it has no caller. Gated off on this host, so design it rather than patch it blind. | `DANA-L1-005`, `DANA-L1-009` (both precondition-gated; both **recurrence 2** in `2026-08-30-moonshot-cert`, see `MC-X1`) | open — **premise changed**: the enterprise data-boundary claim is now on the pricing page, so this deferral is no longer free and must be re-confirmed rather than rolled forward |
| C4 | **Peer-cohort maturity context for the board audience**: "what does L3 · Augmented mean against DORA/DX norms," the board-facing half of C2. May merge into C2. | Dana near-finding (unfiled) | open |

### Declined (recorded so they cannot resurface without new evidence)

| # | Item | Reason | Reopen when |
|---|---|---|---|
| D1 | Add a trajectory/ETA to the fleet Overview (`DANA-L1-004`) | Would multiply the exact unhedged-ETA defect B9 fixes across one more surface. The product is "most careful where it matters least"; make the hedging travel, don't add another ETA. | B9 has shipped **and** recertified, and a Character needs the trajectory on Overview specifically. |
| D2 | Treat the ~8% prompt cap as a defect (`SAM-L1-07`) | Two Characters reached opposite verdicts, and the acquitting one did the arithmetic: *"it's narrating a fully-computed signal set and sampling 22 KB for texture. The pitch survives."* Recorded as a quality **ceiling**, not a gap. The label half is actionable and moved to B7. | Evidence that the sample size changes a score. |
| D3 | "Someone in my role must read the board PDF aloud before it ships" (Dana's process ask) | No human in this loop to assign it to; that is the premise of this repo. `/uat` L2 **is** that read; it is what produced B3 and B9. Converted to method commitment **M1** above rather than a ticket. | n/a, it is a standing rule, not a backlog item. |
| D4 | Wire up `/launch` (unreachable, unlinked, anonymous sign-in prompt) | No Character's job touches it; Tomáš tagged it `unreachable` and filed nothing. | A Character whose journey needs it. |

### Method / fixture

| # | Item | Evidence | Status |
|---|---|---|---|
| M2 | **`env.md` fixture gap.** A seeded org with **≥3 scans of one repo across ≥2 calendar days (≥14-day span)** is required before B9 can reach `resolved-verified`. `seed-org.mjs` scans in a single pass, so `forecastTrajectory` returns null and six generated board PDFs contained zero `Trajectory:` lines. | `DANA-L1-001`/`-002` resolved `uncertain — not reproducible on this host` | **closed 2026-08-31** — arm B did not seed the fixture, it **selected** one: org `public` carries 93 scans across four calendar days and the org shell accepts `?range=custom&from=&to=`, so a window containing exactly two scan days is a zero-write low-data fixture. Recorded in `uat/env.md` |
| M3 | Next run should emit `resolution` on **every** `findings.json` row, including plain `open` ones: 30 of 37 rows carried `null`, so drain §1 had to be reconstructed from prose. | drain §3 | **closed 2026-08-31** — `2026-08-30-moonshot-cert` emits `resolution` on 86 of 86 rows, and a `ceiling` on every closed one |

---

## scan-sweep `--develop --ideas-only` moonshot round (2026-08-29)

Lenses: `feature-scout` + `moonshot-architect` (operator-defined: architecture-grade, category-defining capabilities) over **all 11 context groups / 54 contexts**, L/XL findings only. 45 findings from 11 parallel group scouts, merged into **37 deck items** and triaged one by one in the terminal. Tally: **22 accepted · 5 concept-doc first · 10 deferred · 0 declined**. Full finding bodies (Summary/Description/Flow/Impact/Evaluation, `file:line` evidence) are in `.personas/memory-outbox.jsonl` (30 of 45 emitted — the outbox is past its ingest cap; see the overlay note) and the per-context coverage ledger in `.claude/scan-history/scan-sweep.jsonl`.

Convergence worth noting: four scouts independently proposed the open benchmark corpus (#2), three the control/governance event ledger (#1), two each the forge layer (#4), the guidance arbiter (#15) and GitHub Teams identity/scoping (#21).

| # | Item | Contexts (scout src) | Size | Gate | Decision |
|---|---|---|---|---|---|
| 1 | Governance Evidence Ledger (event-sourced control timeline, as-of-merge pack, control-failed alerts) | GitHub App Installation & Webhooks (02#1); Fleet Alerts & Digests (04#1); Security Posture & Audit Log (05#2) | XL | policy | **shipped-unverified** (wave 3, `29cf87b9`) — awaits its UAT journey; known follow-ups: governance-history backfill, conformance→control-alert call site (W4-O), end-to-end webhook redelivery test |
| 2 | Open benchmark corpus: rubric-versioned snapshots, public dataset + percentile API, federation, consent | Fleet Rollups & Insights (04#4); Portfolio & Public Leaderboard (08#4); Database Client & Schema (10#3); Landing Page Prototypes (11#1) | XL | policy/contract | concept-doc first |
| 3 | Agent-neutral work protocol: claim/brief/report follow-ups over MCP + tokens | Follow-ups Ledger (06#1) | XL | policy | **shipped-unverified** (wave 4) — awaits its UAT journey; known follow-ups: attachRemoteClaim two-concurrent-runs test |
| 4 | Forge-neutral ingestion: GitLab / Bitbucket / Azure DevOps behind the RepoSource seam | Scan Pipeline & Ingestion (01#3); GitHub Repo Data Access (02#3) | XL | contract | **shipped-unverified** (wave 4) — awaits its UAT journey; known follow-ups: LIVE GitLab verification + the 10-org live equality cohort (network run), group listing in import, Repositories forge badge, externalId persistence |
| 5 | Gate-as-code: manifest-declared bar, ratchet/no-regression, one evaluator at pre-push/CI/check-run | CI Gate & Status Checks (01#2) | XL | contract | concept-doc first |
| 6 | Signed maturity attestation (in-toto/DSSE) + verify CLI | PDF & LLM Export (08#2) | L | policy | deferred |
| 7 | AI Trust Center: opt-in tenant-published governance scorecard with signed digest | Marketing About Page (11#2) | XL | policy | concept-doc first |
| 8 | Agent-admission compiler: stance + autonomy tier become enforced per-repo controls | Practices, Governance & Adoption (05#1) | XL | policy | **shipped-unverified** (wave 4) — awaits its UAT journey; known follow-ups: proposal dry-run modal UI, oversight write-path route, requireChecks editor surface |
| 9 | Intervention Outcome Ledger: measured lift per recommendation, fleet- and corpus-wide | Roadmap & Recommendation Tracking (08#1) | XL | policy | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 10 | Two-speed fleet: durable scan queue + credit-free control probes | Org Import, Scan & Watchlist (04#2) | XL | contract | **shipped-unverified** (wave 3, `29cf87b9`) — awaits its UAT journey; known follow-ups: governance-history backfill, conformance→control-alert call site (W4-O), end-to-end webhook redelivery test |
| 11 | Unified LLM meter: every inference lane priced, team/segment showback | Usage Metering (09#1) | XL | policy | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 12 | Purchasable artifacts: one-time Polar products that fulfil an entitlement | Checkout & Plans (Polar) (09#2) | L | contract | deferred |
| 13 | Manifest-as-scan-input: declared-vs-proven capability conformance | AI-Native Standard & Onboarding Skill (03#1) | L | contract | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 14 | `.ai/memory` comes home: per-repo agent memory indexed into Org Memory | AI-Native Standard & Onboarding Skill (03#2) | XL | policy | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 15 | Guidance arbiter / guidance graph: canonical source, projections, verified contradictions, D1 coherence | Maturity Model & Scoring Engine (01#5); AI-Native Standard & Onboarding Skill (03#3) | L | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 16 | Doctor findings as fleet control telemetry: per-check ledger, control matrix | AI-Native Standard & Onboarding Skill (03#4) | L | contract | **shipped-unverified** (wave 1, `5627bf43`) (steps 1–7; `requireChecks` folded into W4-O) — awaits its UAT journey |
| 17 | Work-time registry over MCP: skills, governing subjects, invoke/citation write-back | MCP Server (07#3) | XL | policy | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 18 | Standards conformance ledger: registry signals + registry-map as a fleet dimension | AI Registry Repo (Onboarding & Index) (07#1) | XL | contract | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 19 | Live invoke channel: per-skill/per-repo usage feeding dormancy and outcomes | Skills Registry & API Tokens (07#2) | L | contract | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 20 | Athena as registry curator: PR-proposing actions | Athena Companion (07#4) | XL | policy | deferred |
| 21 | GitHub identity graph sync + scoped membership (teams, auto-RBAC, self scope) | GitHub App Installation & Webhooks (02#2); Members & Access Control (04#3) | L | policy | deferred |
| 22 | Developer-held credential lane: user-to-server GitHub tokens for UC3 | GitHub OAuth & Session (02#4) | L | policy | concept-doc first |
| 23 | Agent behaviour ledger: OTLP sessions as UC3's second sensor | Developer home (UC3 individual care) (05#3) | XL | policy | concept-doc first |
| 24 | Billing account above the tenant: pooled credits, sponsored orgs, resellers | Credits & Entitlements (09#3) | XL | policy | deferred |
| 25 | Org-brief for every lane: playbooks/house pattern/memory in; lessons and declines out | Playbooks (06#4) | XL | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 26 | One improvement ledger: loop lanes become Bought/programme/conformance evidence | Executive Briefing (06#3) | L | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 27 | Remediation economics: cost per verified dimension point, per model, per lane | Local Autopilot & Loop Engine (06#2) | L | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 28 | Durable scheduled drives bound to the programme's cadence and pace | Local Autopilot & Loop Engine (06#5) | L | policy | deferred |
| 29 | Score-input ledger: persisted scan inputs for offline re-score and rubric migration | Scan Pipeline & Ingestion (01#1) | L | policy | deferred |
| 30 | Reproducibility certificate: measured per-model noise bands on anchored scores | LLM Provider Abstraction (01#4) | L | policy | deferred |
| 31 | Signed tenant history bundle: export + import of a scan time series | Scan Persistence & History (10#1) | XL | policy | deferred |
| 32 | Retention compaction: pruned scans age into rubric-tagged digests | Data Retention & Purge (10#2) | L | contract | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 33 | Practice adoption ledger: post-merge drift, versioned house patterns, fleet rollout | Practices, Governance & Adoption (05#4) | L | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 34 | Exemplar Diff: signal-level comparison against a peer repo or cohort | Trends & Comparison (08#3) | L | contract | **shipped-unverified** (wave 2, `8cdcdf84`) — awaits its UAT journey (#26/#27: re-run Dana M1 — the briefing PDF gained a proof line) |
| 35 | Fleet foundation rollout with self-provisioned report-back | First-Run Onboarding Wizard (03#5) | L | policy | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 36 | Git-native improvement channel: lessons mirror, skill Trace, reflect-as-PR | Org Memory (07#5) | L | contract | **shipped-unverified** (wave 1, `5627bf43`) — awaits its UAT journey |
| 37 | Data-bound deck diagrams: every marketing figure is a live public read model | Design System: UI Primitives & Deck (11#3) | L | contract | deferred |

Deferred and concept-doc items stay on this list so they cannot be re-proposed as new; an accepted item is a build direction, not a shipped feature — each still needs its own design/ship-loop milestone and (most carry a `contract` or `policy` gate) a reviewer who is not the author.

### S/M defects the scouts noted in passing (not built — ideas-only round)

- **group 10:** docs/features/data/README.md says 40 models vs data-model.md 48 (drift); ScanDimension lacks (scanId,dimId) index (schema.prisma:640); erase/purge preview reports dims/recs as 0 by design (retention.ts:174-183) but field names imply real counts.
- **group 11:** IndexOrg.tsx:46-48 sells an 'ROI-ranked backlog' while the plan forbids leading with ROI (XS); leaderboard/page.tsx:79-81 promises a link with a plain span (XS); feature-doc-map + AGENTS.md still list src/app/api/badge/** though badge routes are deleted (S).
- **group 01:** noise.ts:9-10 still says guardband ±25 (r8 = 6; stale); context-map brief says 8 dimensions, code has 9; llm-providers.md:739 vs config.ts:81 disagree on which providers are reproducible.
- **group 02:** DEFAULT_TTL_MS in webhook-deliveries.ts is 10 min while the route passes 24h (S); github-app.md/setup.md omit installation_repositories and check_run from the events list (S); source.ts:55-58 comment cites the deleted /connect PrivacyNotice (XS).
- **group 03:** GET /api/report/conformance trend walks up to 1,000 audit rows per request with substring meta match (unindexed); docs/features/onboarding/README.md:31-33 still lists 'Connect & Repo Selection' though /connect was retired; manifest.ts generatedAt is the scan date so a repo can start 'stale' on first doctor run (doctor.ts:233-234).
- **group 04:** reconcileListedRepos never runs for App-installed orgs so renamed/archived private repos burn rescan slots forever; RepoTeam.source github_teams declared with no writer; docs/features/fleet/enterprise.md cites advanceSchedule which no longer exists.
- **group 05:** gate `policyFromParams` drops `minAiGovernedRate` on the no-org-policy path (src/lib/scoring/gate.ts:483-496 vs :474-475); practices.md W6 deviation text says `minedStarter` is not wired into apply, but it is via `resolveHousePattern` (src/lib/practices/apply.ts:72-76, src/lib/practice-artifact.ts:499-513) — doc drift; conformance pack limitations omit that `environment` is latest-scan not as-of-merge (src/lib/conformance/pack.ts:270-299 vs src/lib/db/ai-changes.ts:138-141); `StanceFinding.advisory` is never emitted true so `compliant` is degenerate (src/lib/org/stance.ts:222,238,248,259,274); `TeamStandingSnapshot.standingsJson` is written and never read (src/lib/db/team-standings.ts:59-62); `transferPlaybook` output is ephemeral despite copy promising a Goal/Initiative handoff (src/features/standing/tech-stacks/transferPlaybook.ts:35-80).
- **group 06:** agent.ts:98-103 drops cost/usage/turns from the claude -p envelope; docs/features/org-planning/README.md cites retired routes (/api/org/initiatives, /api/org/simulate) and old component paths; context-map entry points for Live/Playbooks no longer match the feature tree.
- **group 07:** `bySkill` from the usage lane is dropped after the catalog (src/lib/db/org-registry-write.ts:61 persists totals only) so the Registry tab's `invokes30d` can never be attributed; `POST /api/mcp` checks only `mcp:read` and has no memory plan gate (src/lib/athena/grounding.ts:19-24 names it, src/app/api/mcp/route.ts:97-104); `OrgSkillEvent.source` still free text (src/app/api/org/skills/events/route.ts:39); GOLDEN-USE-CASES Phase A telemetry contract (`telemetry/<repo>/<yyyy-mm>.jsonl`) diverges from the shipped `usage/<contributor>.json` lane the indexer reads (src/lib/registry/index-walk.ts:51-58).
- **group 08:** RoadmapSandboxScenarioBar shows projected-vs-actual but nothing writes back to the recommendation timeline; trends/annotations.ts says 'no deploy feed yet' while Deployment rows are persisted and unread; llm-markdown.ts omits scoreIntegrity, governance and aiChanges (relates to B6).
- **group 09:** getCreditReconciliation classifies reversals with /refund/i (credits.ts:508) despite the CREDIT_REASON contract (M); Subscription rows are written by nothing so freeToPaidConversion (kpi-metrics.ts:197-221) can only report 0 (M); public-scan-quota.ts:1,55 says Free is 5 scans/month while plans.ts:190 is 20 (S, adjacent to B8).

---

## UAT drain — `2026-08-30-moonshot-cert` (drained 2026-08-31)

Analysis: [`docs/product/uat-insights/2026-08-30-moonshot-cert.md`](./product/uat-insights/2026-08-30-moonshot-cert.md).
Characters: Dana (VP eng) · Sam (staff eng) · Tomáš (prospective buyer) · Nadia (AppSec lead) ·
Priya (platform lead, ×2 journeys) · Victor (FinOps director). Source run: 86 findings, three L2 arms.
Drain tally: **28 build · 1 concept-doc · 2 method-commitment · 1 decline · 4 covered-elsewhere**.

Ranking in the analysis doc is **recurrence → convergence → voice escalation → impact**, which is a
triage key, not a work queue. ~~**If only three items ship: `MC-B11`, `MC-B2`, `MC-B3`.**~~ — **all
three shipped and recertified on 2026-08-31.**

> **Follow-up drain, 2026-08-31 (post-recertify passes 1+2)** — analysis doc §*Follow-up drain*.
> Two `recertify` passes (`f7bfa8d2..c56ab666`, then `b1992360..afc913c0`) built **18 of this
> section's items** and closed **31 of the run's 86 findings** — 28 `resolved-verified` with live
> evidence, 3 `fixed` behind a named fixture. The rows below are re-stamped with what was *measured*,
> each carrying the ceiling that remains. Follow-up tally: **7 build · 1 concept-doc · 1
> fixed-pending-recert (`MC-B8a`, r15) · 1 routed-by-being-built (`RC-N4` → `MC-B33`) · 5 method**.
> **If only three of the new items ship: `MC-B40`, `MC-B41`, `MC-B36`.**
>
> **Id collision resolved in the same change.** Three builder-appended handoff rows had reused the
> drain's own numbering — `MC-B22`, `MC-B23`, `MC-B24` each named two different items, and
> `recertify.md` cites one of them under three names. The handoff rows are renumbered
> **`MC-B32` / `MC-B33` / `MC-B34`** (the §2 ranking is canonical and keeps its ids) with the alias
> recorded on each. **A handoff row takes the next free number in the section; a drain's numbering is
> never reused.**

**Guardrails binding every item in this section** (this run's ten PROTECT rows; full text in the
analysis doc §2 "Strengths → do-not-touch guardrails"). G1–G9 from the 2026-08-10 section remain in
force.

- G10: D1 stays **claim-scored** — no guardband reintroduction; the coherence read stays itemized; `coherence: null`, never 0.
- G11: pricing stays numeric/anonymous/one-click with the **self-host band above the grid**; no "talk to sales" on Starter/Team.
- G12: briefing fixes land **in the shared composers**; never inline a composed sentence into a renderer.
- G13: branch points never fold into the bought number; `briefingProofLine` keeps returning **null**, never "0 · 0".
- G14: keep D2's `MIN_SAMPLE_FRACTION` fairness floor, and keep **surfacing** the model's objections to the product's own detectors.
- G15: the three-state vocabulary (`unmeasurable` first-class, `unchecked` outranking `pass`, the "Not judged" tile) is extended, never collapsed.
- G16: never fold advisory counts into D9; keep the degraded *"NOT a clean bill of health"* banner and the audit CSV's `-PARTIAL` honesty.
- G17: `observed` keeps winning on manifest regeneration; keep the owner/admin blast-radius split and one typed `owner/repo` per write.
- G18: no confidence theatre in the loop — every price cell keeps its `n`, unproductive spend stays its own line, `pickDriveModel` keeps refusing.
- G19: a null cost is never rendered as `$0.00`; the unpriced-call count stays beside the money.

### Build

Impact is the finding's own `frequency / reachability / trust-erosion` triple.

| # | Item | Evidence | Impact | Size | `standard:` | Status |
|---|---|---|---|---|---|---|
| MC-B1 | **The board forecast must replace its hedge, not delete it — and the basis composer must acquire its caller.** On `lowData` the briefing nulls `forecastConfidence` and every renderer's hedge is guarded on it, so the least trustworthy fit renders most confidently, on the artifact with the org's name on it; the shared presentability gate (`isProjectable` / `forecastInsufficiency`) is never consulted on the briefing or digest path; and `forecastBasis` — which composes the exact sentence, is tested three ways and whose docstring names a caller — has zero non-test callers. Honors **G4/G12/G13**. | `DANA-L1-001` (**recurrence 3**, prior `B9`) · `DANA-L1-002` (rec 2) · `DANA-L1-013` · `DANA-L1-014` (`hypothesis` for the user-visible consequence — the `compacted` grep is unambiguous, no purged-history fixture exists) · arm B: Briefing *"Climbing at +35/wk"* vs Delivery *"Not enough history to project: 2 distinct scan days"*, one click apart · Dana: *"The ETA I would delete from the slide before presenting."* | high/high/high | M | `metric-forecasting/projection-presentability-gates` (+`fit-confidence-honesty`) | **resolved-verified 2026-08-31** (`4aa12080`; recertify pass 1, `f7bfa8d2..c56ab666`) — `DANA-L1-001`/`-002`/`-013` all closed live: the low-data window refuses (*"Not enough history to project: 2 distinct scan days"*), the 90d window carries the basis (*"fit over 8 scan days across 75 days"*) on the card, the markdown **and the decoded PDF**, and the briefing now speaks Delivery's exact sentence. **Ceilings:** the `compacted` tail has still never rendered; the low-data window still prints a `Change vs … start: +2` delta ungated by the same rule; the **digest** path was not exercised (`cronSecret:false`) → `MC-B27` |
| MC-B2 | **Pass the real predicate to the landing dialog, and give all three scan doors one answer.** The server un-walled the anonymous public scan and the hero still locks on `authGateEnabled()`; `/report?repo=` has no gate at all and starts a live 155 s scan on page load. Honors **G11**. | `TOMAS-L1-01` (**blocker**, recurrence 2, prior `B2` — server half only) · `TOMAS-L1-08` · arm A: dialog sign-in panel + cookie-less `POST /api/scan` → 200 in 3.3 s, 30 s apart · Tomáš: *"a wall that isn't there, painted onto the door people actually use."* | high/high/high | XS | `plan-entitlements/capability-gate-predicates` | **resolved-verified 2026-08-31** (`f6cbe4a2`; pass 1) — anonymity asserted from ARIA first, then the hero CTA opens the **scan form** with its QuotaMeter; all three doors read live and agree. **Ceiling:** they agree because the wall is OFF — door 2's copy is still a literal (`ColdScanGate.tsx:52`), so `PUBLIC_SCAN_SIGN_IN_REQUIRED=1` re-splits them |
| MC-B3 | **The provenance track must read the per-dimension band, and stop drawing a mechanism that does not exist.** Chip says DOUBLED, both `<title>`s say ±6, the `<rect>` measures 28.32 on every dimension — the widened band is not drawn; and D1/D4/D9 get a guardband and an "LLM judgment" tick for a score the model cannot move that way. Delete the dead `DimensionCard.tsx` in the same PR or the fix must be made twice. Honors **G5/G10/G14**. | `SAM-L1-02` (recurrence 2, prior `B6` half-shipped) · `SAM-L1-11` · `SAM-L1-STR-04` · arm A `shots/armA-dimloop.json` (D1 `0/3/0` vs D8 `0/4/2`) · Sam: *"The number's good. The diagram explaining the number is lying to me."* · magnitude corrected to **±6/±12** (same staleness as the scan-sweep's `noise.ts:9-10` note — reference, don't re-file) | high/high/high | S | `judgment-guardbands/score-guardband` (+`judgment-integrity-record`) | **resolved-verified 2026-08-31** (`904bcc21`; pass 1) — D2/D6 band rects measure **56.64** against 28.32 on the ±6 dimensions (exactly 2×), the blend weight renders on every blended dimension, and D1/D4/D9 draw no band and no judgment tick. **Ceilings:** no facet table on the claim-scored panel (unexercised — no non-zero `claimPoints` fixture); and the page now states one fact in two units → `MC-B35` |
| MC-B4 | **Hand over the permalink, with a level line — and serve Sam's third job rather than retiring his criterion.** The scan ends on `/report?repo=…`; a whole-DOM sweep finds one hit and it is a share-card PNG, while the Free card sells *"Public report permalink"*. **Decision recorded (`SAM-L1-12`): option (b) — serve the job. Criterion #6 stays scored and the character file is unchanged.** | `SAM-L1-04` (**recurrence 3**, prior `B5`) · `SAM-L1-12` · `RV-SAM-L1-03-badge` ceiling · Tomáš: *"They are billing a feature — at $0, but billing it — that the product never hands you."* · Sam: *"I can't put a query-string URL in a README and I can't put a PNG in CI."* | high/high/low | S | none | **resolved-verified 2026-08-31** (`9d85f01a`; recertify pass 2, `b1992360..afc913c0`) — three separately-copyable payloads (permalink, commit-pinned, README markdown) with the level line stated twice; a cookie-less `GET /report/sindresorhus/slugify` returns 200 and 54 KB. **Ceiling:** option (b) is a markdown link — there is still no image/SVG endpoint for a README that wants a rendered shield |
| MC-B5 | **State the real free-scan allowance everywhere it is stated, and rename the 429's tier.** "Unlimited free public scans" + "always free and unmetered" against a rolling-30-day limit of 5; the 429 upsells "Pro" for a tier the UI calls Starter; `.env.production` documents a **weekly** gate the code does not read. Honors **G11**. Ships with `MC-B2`. | `TOMAS-L1-02` (recurrence 2, prior `B8`) · `TOMAS-L1-09` · arm A: Free card vs `◷ 4 free public scans left`, `/api/quota limit:5` · Tomáš: *"Unmetered, next to a meter."* · adjacent to the scan-sweep group-09 note (5/month vs `plans.ts` 20) | high/high/high | S | `plan-entitlements/id-vs-label-split` (+`price-book-authority`) | **resolved-verified 2026-08-31** (`21bef0ed`; pass 2) — **proved by moving the source**: the arm booted with `PUBLIC_SCAN_MONTHLY_LIMIT=1` and the Free card, the dialog meter and the 429 all read **1**; the 429 names Starter. The allowance has ONE source (`src/lib/public-scan-limit.ts`). NOTE: the stale weekly-gate doc is in `.env.example`, not `.env.production`. **Ceilings:** the credit matrix on the same page still renders `Unlimited` → `MC-C2`; the derived number is not pluralized → `MC-B38` |
| MC-B6 | **Label `/about`'s ROI simulator illustrative, or delete it and promote the register.** Eight invented repos at a self-declared non-production weighting, still with no rendered label. | `TOMAS-L1-04` (recurrence 2, prior `B12`) · Tomáš: *"Take it out. The register of real repos does more for you than that thing ever will."* | high/high/med | XS | `executive-reporting/provenance-caveats` | **resolved-verified 2026-08-31** (`27a722ee`; pass 2) — `ILLUSTRATIVE · 8 SAMPLE REPOS, DEMO WEIGHTING — NOT CUSTOMER DATA` renders under the grid and the metric row. **Labelled, not deleted** (the register is server-fetched on `/` while `/about` is a client deck). **Ceiling:** the *"a live scan substitutes for proof"* limb is untouched |
| MC-B7 | **Carry the workflow FILENAME into the D3 evidence lines.** D3 cites nothing, D5's linter/formatter and all of D8 likewise — but L2 softened and **relocated** it: the LLM narrative above the list already names the paths, so the deterministic detectors lag the prose, not the page. Honors **G6**. | `SAM-L1-01` (recurrence 2, prior `B4` partly shipped) · arm A expanded-DOM capture · Sam: *"a paragraph that cites its sources and a list labelled EVIDENCE that doesn't."* | high/high/med | S | `scoring-rubrics/score-explanation` | **resolved-verified 2026-08-31** (`13410fcf`; pass 2) — needed a **fresh live scan** (evidence strings are written at scan time): `CI runs tests` → `CI runs tests (.github/workflows/main.yml)`, and D3's CI-presence award and D6's formatter likewise. **Ceiling:** judged on one absence-shaped repo; a repo with several workflows would test whether the D3 cluster can name a filename at all |
| MC-B8 | **Recurrence papercut batch.** (a) additive `firstStep` field so the concrete move leaves the rationale prose (**G2**); (b) one outcome word per "Flagged for review" row — widened / structurally ineligible / lost to the budget; (c) `scoreLabel` covers 5 of 8 providers and the gap **widened** — `local`, the self-hosted path, is the slowest and falls through; (d) suppress the register's `0 public repos rated` counter at zero. | `SAM-L1-05` (rec 2, prior `B14`) · `SAM-L1-06` (rec 2, `B15`) · `SAM-L1-08` (rec 2, widened, `B15`) · `TOMAS-L1-05` (rec 2, downgraded to polish, `B13`) | mixed | XS | `remediation-roadmaps/invitational-framing` · `judgment-guardbands/judgment-integrity-record` · `status-vocabulary/number-formatting` · none | **(b)(c)(d) resolved-verified 2026-08-31** (`0d088f41`, `97d48243`; pass 2) — each flagged row ends in `widened` with its clause; `SCORE_STEP_LABEL` is a total `Record` over `ProviderName` including `local`; a genuinely empty database renders no repos-rated counter at all. **(a) `SAM-L1-05` stayed OPEN at pass 2** — `firstStep` shipped end-to-end and bumped the rubric to r14, and the first live r14 reading returned it on **0 of 9** roadmap items because the ROADMAP COVERAGE block never asked for it (`RC2-N1`). **Now fixed 2026-08-31 (`ece52101`, r15) — pending live verification**: the mandate asks for the field in the invitational voice with `Omit "firstStep" only when no single concrete move exists`. One fresh scan closes it. **Ceilings:** only (b)'s `widened` branch was exercised; (c)'s `local` arm is compile-verified only |
| MC-B9 | **Round-trip `requireChecks` through the policy form, add its editor, and stop rendering structurally-impossible zeros as measured meter rows.** The field renders read-only six rows above an editor that cannot set it and is **silently deleted** when an owner saves any unrelated field; the app's own audit row names it under `previousPolicy` and says nothing in `policy` or `status`. Four parts: merge-not-replace (or round-trip unknown fields), the editor, reconciliation that can see a field the form never sent, and `PRIYA-L1-02`'s "not judged fleet-wide" rows. Rename the Delivery tile to *"Required status checks"* **first**. **The editor surface itself is covered-elsewhere — moonshot #8's known follow-up (`MC-X2`); this row adds only the other three halves.** | `NADIA-L1-07` + `PRIYA-L1-01` (**convergence**, deduped) · `PRIYA-L1-02` · `PRIYA-L1-07` · arm B before/after captures + the audit row · Priya: *"it actively undoes the write the next time anyone touches the form."* | med/high/high | M | `quality-gates/enforcement-binding` (+`policy-projection`, `unmeasurable-criteria`) | **resolved-verified 2026-08-31** (`a96dd03a`; pass 1) — the policy round-trips **byte-identical** through a form save of an unrelated field; the restore POST names the dropped bar (*"required controls … control.prepush.lint, guardrail.never-commit"*); the two fleet rows read *"not judged fleet-wide"* while two required controls were actively declared; the Delivery tile reads REQUIRED STATUS CHECKS. **Ceilings:** no editor — an owner still cannot set or clear a required control (`MC-X2`); two rows are **earned zeros** with the reasoning only in code → `MC-B37`; `unjudgedBarsDeclared()` has no caller → `MC-B36` |
| MC-B10 | **Name the three "controls" catalogues distinctly on screen and cross-link them**, and stop using SEALED for two things on one tab. Security › "Control matrix" (D9 battery) · Passports › Controls (doctor check-ids) · Governance › "Control observations" (branch-protection ledger); plus the AI-stance perimeter's SEALED. | `NADIA-L1-08` + `PRIYA-L1-07` (**convergence**, same collision from two tabs) · arm B: SEALED as a posture control | med/high/low | XS | `status-vocabulary/vocabulary-chain-integrity` | **resolved-verified 2026-08-31** (`527b5973`; pass 2) — the three catalogue headings all render and each cross-links the other two; the ledger says "chained"; the perimeter band still says SEALED. **Ceiling:** "controls" is still the noun in all three headings — they are distinguished, not renamed |
| MC-B11 | **Separate an agent's claim from the rescan's verdict, and make every close say which rule fired.** `resolved` is written from `closedIds` ← `parseResolvedIds(commit messages)` ← trailers the lane wrote from the agent's own `RESOLVED:` lines — *"the trailer is a CLAIM, never a verdict"* per `lane-commit.ts`'s own header — and the cockpit prints it as *"closed by the rescan"*. Route `rescanWorktree` through the same `decideInProgress` movement witness `scans-persist.ts` uses; store a `verified` discriminator; render *"claimed resolved — awaiting the rescan"* in a distinct tone; fix the test whose title contradicts its assertion. Honors **G13/G18**. | `PRIYA-L1-702` — **highest impact triple in the run** · arm C: 46/46 tautology; cockpit *46 closed* vs `backlog {done:0}`; a session **killed by timeout with 0 item verdicts** still trailed all 5 armed ids · Priya: *"I would still roll this out. I would not show anyone the verdict panel until it says which of those two numbers it means."* | high/high/high | M | `remediation-handoff/evidence-based-auto-close` | **resolved-verified 2026-08-31** (`d165da23` + `MC-B33`/`9e8906f1`; **open at pass 1, closed at pass 2**) — pass 1 measured the mechanism landed and the symptom unchanged (36/36 rows still `verified:true`, because `listRunOutcomes` joined verification back out of `closedIdsJson`); pass 2's sweep inverts it: **0 true / 40 false**, every false row `verifiedAt: null`, the cockpit reading *"claimed resolved — awaiting the rescan"*, `grep -c "closed by the rescan"` → **0**, and cockpit and ledger no longer contradict. **Ceilings:** the positive half is unit-verified only (no live row has earned a stamp yet); the lane rails and sheet header still say "closed" for two other quantities → `MC-B41` |
| MC-B12 | **Sum every lane into the `/usage` headline tile, or qualify it "scan lane only".** The tile prices the scan lane and understates the page's own itemization by **78 %**; the page prints the real total three rows below. Honors **G19**. | `VICTOR-L1-05` — **voice escalation**, filed `minor` at L1 (F5), upgraded to `major` by live measurement · arm B: tile `$25.90` vs lane/team sum `$115.28` (itself a floor: 34 of 53 calls unpriced) · Victor: *"I read the big number first. The big number is the small one."* | high/high/high | S | `cost-metering/spend-observability` | **resolved-verified 2026-08-31** (`e2eaf39b`; pass 1) — `$33.17 + $178.34 = $211.51` exactly across tile, lane table and team table on one screen, with the qualifier and the floor (*"45 calls in this period could not be priced"*); the API agrees. Arm B's 78 % gap is **zero**. **Ceiling:** 45 of 76 local-lane calls still price at $0 — the qualifier names the count, not the exposure; per-org attribution is still one bucket → `MC-B19` |
| MC-B13 | **Give the control ledger's three authored contracts their consumers, and read the disclosure the route already computes.** `failMeans` (0 consumers — so a red "not operating" ships without *"NOT a statement that the repo is insecure"*), `descriptor` (0 hits — "Visibility · operating · public", "Not archived · not operating"), `stateTone` (re-implemented inline), and `truncated`/`limit` (the card never calls `/api/org/controls`); plus state-from-newest-400 against coverage-from-oldest-2000. Honors **G15/G16**. | `NADIA-L1-02` · `NADIA-L1-03` (**voice escalation** on a `minor`) · `NADIA-L1-01` · Nadia: *"If I screenshot this table for the CISO, I have just told him nine repositories failed a security control. They did not."* · wiring halves confirmed statically; numeric consequence `uncertain` pending `MC-M3` | high/high/high | S | `status-vocabulary/vocabulary-chain-integrity` + `metrics-rollups/aggregate-honesty` | **fixed 2026-08-31** (`9f0f83ce`; pass 2) — **`uncertain — not reproducible on this host`, not `resolved-verified`.** The wiring audit that produced the finding now answers the other way (`failMeans` and `descriptor` have live consumers and the card's footnote renders), but **no row on this host is red and no descriptor control is observed**: all 16 observations are `unmeasurable` (no GitHub App) and `/api/org/controls` is GET-only, so the fixture cannot be built over HTTP. Blocked on `MC-M3` |
| MC-B14 | **Give the seal recipe rows to run on and a door to run it from.** `?format=csv` on `/api/org/controls` emitting `DIGEST_FIELD_ORDER` verbatim + a download link; a rendered *"Ledger integrity: chain verified through … · N days unsealed"* with a Verify action (today the only reference is a non-interactive `<code>` string **inside the non-empty branch**); seal from the existing rescan/digest cron instead of as a side effect of a read; return `sealBacklogRemaining` and derive `unsealedDays` from a DISTINCT-day query; add the seal root to the pack manifest. Honors **G16**. | `NADIA-L1-04` · `NADIA-L1-05` · `NADIA-L1-06` · `NADIA-L1-11` · arm B: `?format=csv` → `200 application/json`; `/api/audit/verify` wrote an audit row and sealed nothing · Nadia: *"I am told exactly how to recompute a chain and given no rows to recompute it over."* | high/high/high | M | `audit-logging/tamper-evidence` (+`append-only-design`, `retention-and-partitioning`) | **resolved-verified 2026-08-31** (`30f53490`; pass 2) — `?format=csv` returns real CSV headed by `DIGEST_FIELD_ORDER`; the integrity strip with **Verify now** + **Download observation rows (CSV)** renders in the non-empty branch (kiro) *and* the empty one (public); `/api/audit/verify` returns `sealBacklogRemaining` and no longer seals as a side effect of a read; the pack manifest carries a `## Ledger integrity` section that honestly says there is no root yet. **Ceilings:** the export is the rows, not the chain; the retention consequence needs ≥2 closed UTC days of observations |
| MC-B15 | **Render `economics` on the outcome sheet row.** Per-lane ¢/point is computed, serialized and shipped to the browser and read by nothing (`grep -rn "\.economics" src/features src/app` → 0, live); the org-wide average that does render is what hides a **$10.19-for-0-points** lane. Honors **G18**. | `PRIYA-L1-704` (severity normalised: filed "medium", recorded `major` on impact) · arm C: `microsPerVerifiedPoint: 156684150` on the wire, zero `¢` above the org price list · Priya: *"the one figure I would put in front of a VP … is the one figure the panel omits."* | high/high/med | S | `cost-metering/spend-attribution` | **resolved-verified 2026-08-31** (`de0ca559`; pass 2) — the $-for-0-points case reads **`$7.60 · 0 pts`**: the spend is stated beside the zero, beside (not instead of) the org-wide average that hid it |
| MC-B16 | **Exclude remote-executor runs from the stale sweep, and clear `claimActor`/`leaseUntil` on release.** `markStaleRunsStopped` selects `{phase:"running"}` and filters only on `!isLive(id)`; `executor` exists on the lane and appears nowhere in the sweep; `startRemoteRun` documents that it never creates a registry entry, so `isLive` is false by construction and `route.ts:58` fires the sweep on every GET. **The remote consequence is `hypothesis` — `uncertain — not reproducible on this host`, never refuted.** | `PRIYA-L1-701` (**blocker**; reachability `low` today, `high` the day `MC-B17` lands) · arm C: a live LOCAL run survived 6 reads over ~24 s *because of* its registry entry — the isolation, not the excuse | high/low/high | S | none (adjacent law: `failure-not-empty-success`) | **resolved-verified 2026-08-31 on the local control** (`d3b026d9`; pass 2) — a live local run survived ~35 minutes and ~30 cockpit reads. **The remote consequence stays `uncertain — not reproducible on this host` and was never passed** — it still needs `MC-M5` |
| MC-B32 | **Point the spend-anomaly digest alert at the all-lane total.** `extra-alerts.ts:159` + `alerts.ts:943` still feed the anomaly check from `estimatedCostUsd` (scan lane only), so the alert understates exactly the way the `/usage` tile did before MC-B12; `allLanesCostUsd` now exists on the same summary — a one-line swap plus threshold sanity. | MC-B12 handoff (scope deliberately left; the tile fix makes the alert the last reader of the understated figure) · **renumbered 2026-08-31: was `MC-B22`**, which already named the `NEXT_PUBLIC_SOURCE_REPO_URL` row below | med/high/med | XS | `cost-metering/spend-observability` | **built 2026-08-31** (`59ebea64`) — pending recert |
| MC-B33 | **`LaneItemOutcome.verifiedAt` written at adjudication time.** MC-B11's `verified` is a read-time join against the lane's `closedIdsJson`; exact for new rows, but historical rows written when `closedIds` was the raw trailer set read `verified: true` on ids nothing adjudicated. A write-time column makes old rows distinguishable and removes the join. Schema change (contract gate — owner review). | MC-B11 handoff · **renumbered 2026-08-31: was `MC-B23`**, which already named the exemplar-diff `nameKey` row below. `recertify.md` pass 2 cites this item as *"MC-B23 = `verifiedAt`"* and pass 1's `RC-N4` cites it as `MC-B19` — both mean **this** row | low/med/med | S | `remediation-handoff/evidence-based-auto-close` | **resolved-verified 2026-08-31** (`9e8906f1`, lane α; pass 2) — **closes `MC-B11`**. `lane-outcomes.ts:156-173` reads `verified: row.verdict === "resolved" && verifiedAt !== null` — the row's own stamp, written by the rescan when it rules — so a row nothing adjudicated can no longer borrow the lane's word for it. Sweep: 40 resolved outcomes, **0 verified true**, all `verifiedAt: null`. **Ceiling:** pre-fix lanes are not backfilled, so the rails still print `closedIds.length` "closed by the rescan" → `MC-B41` |
| MC-B34 | **Gate the personal-workspace trajectory card on `isProjectable`.** `PersonalOverview.tsx:115` filters on `r.forecast !== null`, not the shared presentability gate, so a sub-gate fit still renders a card — hedged (it carries its own low-data caveat) but ungated, now the only forecast surface not behind `composeTrajectory`. | MC-B1 handoff (no UAT evidence; found by the fix's own sweep) · **renumbered 2026-08-31: was `MC-B24`**, which already named the exemplar-diff discovery-path row below | low/med/low | XS | `metric-forecasting/projection-presentability-gates` | **built 2026-08-31** (`1ce60a0e`) — pending recert |
| MC-B17 | **Teach `repoGate` to consult `listOrgAdmissions`, and validate an admitted repo against the org's WATCHED set.** C-4: the claim gate reads the **derived** autonomy tier and never sees the recorded, overridable decision — an owner who records T2 still gets a T0 refusal. C-5: `repoUnderOrg` requires `owner === org`, so org `kiro` can never admit its own `xkazm04/*` repos and the whole #3 remote protocol is permanently unreachable for it. **Either one unblocks `MC-B16`'s fixture** — the cheapest route to a verdict on a blocker. | `PRIYA-L2-C4` · `PRIYA-L2-C5` (both **new at L2**, both found while building a fixture) · arm C: live MCP `claim_followups` T0 refusal; `POST /api/org/admission` rejected before any write | med/high/high | S | `mcp-tools/authentication-and-scoping` + `remediation-handoff/handoff-tenancy-and-idempotence` | **`PRIYA-L2-C5` resolved-verified · `PRIYA-L2-C4` fixed** 2026-08-31 (`99b290a5`; pass 2). C-5: a three-way **zero-residue gate probe** on the real org proved tenancy — `kiro` + `xkazm04/kp` now passes `repoUnderOrg` while `kiro` + `facebook/react` is still refused — and the write was then exercised end to end on the demo org (200, `derivedTier: T0` / `grantedTier: T1`). C-4 is code-verified only: no live MCP claim was exercised. **Ceiling:** there is **no revoke door** — GET and POST only → `MC-B40` |
| MC-B18 | **Carry `rubricVersion` onto `RegisterEntry` and qualify a stale-rubric row the way a mock row already is.** The codebase states in writing that r10 and r11 numbers are not comparable and gates on the field in the corpus filter, the outcome ledger and the digest keys; the public leaderboard has no rubric predicate, column or note. Cache invalidation on a bump does not re-scan. | `TOMAS-L1-11` · `uncertain` at L2 (single-rubric host; production almost certainly qualifies) · Tomáš: *"Same instinct, one surface short."* | med/high/high | S | `scoring-rubrics/rubric-stability` | **built 2026-08-31** (`0c676a17`) — pending recert |
| MC-B19 | **Link the showback CSV, then make team attribution real.** In order: (a) a third export button beside CSV/JSON — the artifact is built, correct and reconciling, and `grep showback` returns only the route; (b) pass `teamKey` from the local lane, which already has the repo, so `Spend by team` stops reading 100 % "Org-wide"; (c) **after (b)**, the per-lane × per-team matrix spec #11 promised. Honors **G19**. | `VICTOR-L1-02` · `VICTOR-L1-04` · `VICTOR-L1-03` · arm B: the live showback body, reconciling to `$115.28` · Victor: *"I would have gone to the vendor and been told it doesn't exist."* | med/high/med | S | `cost-metering/spend-attribution` (+`spend-observability`) | **built 2026-08-31** (`82a4db82`) — pending recert |
| MC-B20 | **Stop dropping every non-scan model call for an org whose slug is literally `public`.** `meter.ts:219` uses the string as the anonymous-funnel sentinel; a tenant on that slug burns inference and shows `$0` forever. Refuse `public` as a creatable slug, or gate the drop on the funnel org's **id**. | `VICTOR-L2-01` (**new at L2, discovered by driving**) · arm B: a real 12.1 s claude-cli turn recorded nothing; `kiro` proves the lane path works · it silently invalidated one L2 arm before the cause was found | low/low/high | S | `cost-metering/usage-ledgers` | **built 2026-08-31** (`24e68c6a`) — pending recert |
| MC-B21 | **Name the two currencies under the allotment meter.** *"Unused credits roll over. They never expire"* sits under a header reading **"Monthly allotment · 150 credits / mo"**, which resets on the 1st; only the purchased pool persists. Confirm live on the same fixture as `RV-B1`. | `VICTOR-L1-01` · Victor: *"an idle month burns 150 scans of headroom I paid $10 for. That's my pet peeve verbatim, and it flips the math."* | high/high/high | XS | none | **built 2026-08-31** (`29227e76`) — pending recert |
| MC-B22 | **Set `NEXT_PUBLIC_SOURCE_REPO_URL` in the production environment, and make the self-hosting guide a link.** Absent from both committed production env files, so all four consumers degrade at once; a raw-HTML sweep of `/` and `/pricing` finds **exactly one** github.com URL each — the footer's Feedback issue tracker. Build-time inlined: it must be set at build, not at runtime. Honors **G11**. | `TOMAS-L1-10` · arm A ARIA: *"Self-hosting guide: docs/SELF-HOSTING.md"* as a **text** node · Tomáš: *"a page that makes its biggest claim and doesn't say where."* | high/high/med | XS | none | **built 2026-08-31** (`070439ec`) — pending recert |
| MC-B23 | **Difference exemplar evidence through `nameKey`/`signalName`, keep the raw string for display.** Exact normalised equality over strings B4 made repo-specific two days earlier: a repo **with** a test framework is told the exemplar has one; count-bearing lines land in both `absentSignals` and `aheadSignals`; cohort consensus fragments below support. The normalizer written for exactly this sits in the neighbouring module and is not imported. | `SAM-L1-10` (`major`, reachability low behind three walls) · Sam: *"a wrong transfer recommendation is the one output a staff engineer will forward to their team."* | high/low/high | S | `diff-comparison/semantic-level-selection` | **built 2026-08-31** (`14191ea1`) — pending recert |
| MC-B24 | **Give the exemplar diff a discovery path from the report, and stop labelling 500 public-corpus repos "Your repos".** The only inbound link is a time-vs-time "What changed →" gated on two scans and carrying no `?against=`; `readableOrgForOwner` resolves a non-member to the shared public org so `ORG_CANDIDATE_CAP = 500` corpus repos render under "Your repos" with an "Org best" that means "best in the public corpus". The sign-in and DB walls are **not** the finding. | `SAM-L1-13` | med/low/med | S | `diff-comparison/pair-and-baseline-selection` | **built 2026-08-31** (`a4fb2681`) — pending recert |
| MC-B25 | **Persist `stopRequested` and render a "stopping…" state with its horizon; stop putting a session's failure mode in the commit subject.** The run read `RUNNING` for **19 min 43 s** after the operator pressed stop, settling only at the 20-min agent timeout; 1605 insertions across 15 files landed under `fix: Agent session exceeded 20 min and was stopped`. Carries the prior run's L2-B-02 forward (agent config rendered with no engine label). | `PRIYA-L2-C6` · `PRIYA-L2-C7` (both new at L2, both measured) | high/high/med | S | `agent-cli-transport/child-observed-posture` | **built 2026-08-31** (`6e33cadf`) — pending recert |
| MC-B26 | **Stop the cockpit denying a capability the deployment ships.** `LiveTab` loads runs only when `selfHosted()`, so a cloud owner's armed remote runs never render and the rail falls through to a setup panel whose copy says the loop exists only on a self-hosted Ascent — contradicted by that deployment's own POST route. Ideally also load runs for cloud owners and drop `selfHostGuard` from `propose`. | `PRIYA-L1-703` · not attempted at L2 — `uncertain`, needs a cloud-shaped host (`MC-M5` adjacent) | high/low/high | M | `plan-entitlements/deployment-mode-short-circuit` | **built 2026-08-31** (`ead78377`) — pending recert |
| MC-B27 | **Pass the digest's empty controls array through, and carry `controlCoverage` with the block.** The three-state contract (`undefined` = say nothing · `[]` = "we looked and none failed" · rows = these failed) collapses to two at its only production caller, so a clean week is byte-identical to an unpopulated ledger; and the digest reimplements the forecast headline with no hedge in any branch. The digest is the artifact Dana reads **instead of** opening the app. | `DANA-L1-015` · `uncertain` — needs cron + ledger fixture (`MC-M3`) | med/low/med | S | `alerting/periodic-digest` | **built 2026-08-31** (`9f3c7b35`) — pending recert |
| MC-B28 | **One org-wide queue-depth line on the Repositories tab.** `queueDepth()` reaches only the two cron routes' JSON; the residual gap of moonshot #10, whose closure is otherwise confirmed (`RV-M10`). | `VICTOR-L1-07` · Victor: *"I'd see 400 per-row 'queued' tags before I saw the number 400."* | med/high/low | S | `metrics-rollups/aggregate-honesty` | **built 2026-08-31** (`9ad16344`) — pending recert |
| MC-B29 | **Make the rollout copy describe the path the product now automates, and land the promised column.** (a) the generated-workflow comment and the control-matrix empty state still describe hand-wiring two secrets the Repositories tab provisions in one click; (b) chip `schemaAhead` and disclose parse **redaction** notes (0 consumers under `src/features`); (c) land spec #35 handoff 2's report-back column on the capability matrix — `getFoundationRollout` has one consumer, on a different tab; (d) one sentence saying the copied CI snippet is a **snapshot** (tightens propagate, loosens do not); (e) one sentence naming which artifacts an admission decision writes; (f) pass `named` on the two evidence-pack CSV links. Honors **G17**. | `PRIYA-L1-03` · `PRIYA-L1-04` · `PRIYA-L1-05` · `PRIYA-L1-06` · `NADIA-L1-09` (disclosure half only — the dry-run modal is `MC-X3`) · `NADIA-L1-10` · Priya: *"the thing IS easy; the words aren't."* | med/high/low | S | `conformance-checking/declared-then-proven` + none | **built 2026-08-31** (`f200a8ce`) — pending recert |
| MC-B30 | **Loop honesty batch.** (a) log the drive's evidence-led model switch with its basis (`n` and the prices compared) — today an unexplained substitution of the configured model; (b) carry the org's standard into the **remote** brief, or disclose in `live.md` that remote lanes work un-briefed (a remote close can never stamp `PlaybookApplication` adoption evidence); (c) `live.md:871` still points at a known gap #3 correctly deleted. Honors **G18**. | `PRIYA-L1-705` · `PRIYA-L1-706` · `PRIYA-L1-707` (*"the miniature of the failure mode"*) | low/med/med | XS | `agent-cli-transport/dated-capability-matrix` + none | **built 2026-08-31** (`c91a05cc`) — pending recert |
| MC-B31 | **Two meter-edge honesty nits and the `/usage` footer.** (a) `meterLane` writes `inputTokens ?? 0` inside the any-token-present spread, against the module's own null-never-zero rule; (b) an unknown lane string is dropped from `byLane` while still counted in `byTeam`, so two panels on one page can disagree about the same call total with no "unknown lane" row; (c) the footer unconditionally tells a fully-attributed private org that attribution has not activated, on a billing page with no route to the price. Honors **G19**. | `VICTOR-L1-08` · `VICTOR-L1-06` | low/med/low | XS | `cost-metering/usage-ledgers` | **built 2026-08-31** (`d6370d21`) — pending recert |

#### Follow-up drain rows (recertify passes 1+2, 2026-08-31)

Routed from `recertify.md` §*New findings for the next drain* — these have no other door into the
backlog. Analysis: the same doc's **§Follow-up drain — 2026-08-31**, items F1–F8. Ranking there is
recurrence → convergence → voice escalation → impact; none is recurrence-2, but three are
**fix-created defects** (a defect introduced by the fix for an earlier finding), which is this run's
signature failure class and outranks first-raised items of equal impact.

| # | Item | Evidence | Impact | Size | `standard:` | Status |
|---|---|---|---|---|---|---|
| MC-B35 | **One unit for the blend weight on the report page.** The `ScoreIntegrityChip` header reads *"integrity · widened D2, D6 · blend 95%"* while every provenance track on the same page reads *"Blend weight 57%"*. Both are correct — 95 % is the realized fraction of the configured weight (0.57 against 0.6), 57 % the absolute weight — and the chip's tooltip reconciles them **inside a hover**, on the exact surface whose prior finding was *"the page states three things about one number"*. Print one unit, or label the chip *"95 % of the configured weight"*. **Fix-created defect of `MC-B3`.** Honors **G5/G10/G14**. | `RC-N1` (recertify pass 1) · `shots/recert-B3-dims.text.txt:28` · `shots/armA-dimloop.json` · ceiling (b) of `SAM-L1-02` | med/high/low | XS | `status-vocabulary/number-formatting` | **built 2026-08-31** (`5c24ba97`) — pending recert |
| MC-B36 | **Call `unjudgedBarsDeclared()` — the escalation is computed and rendered nowhere.** `governanceReasons.ts:52-60` exports a function that computes *"this org's stored bar carries a criterion the fleet view cannot judge"*; the rendered row prints the same em-dash sentence whether or not the operator has declared one. The strongest version of `PRIYA-L1-02` was a lead who had **just set two required controls** and now reads a correct sentence that does not acknowledge she has skin in it. Append *"— you have declared 2 of these; the per-repo gate enforces them"*. **An `MC-M1` member created inside the PR that closed the finding about zero-consumer fields** — that, not the severity, is the ranking argument. Honors **G15**. | `RC-N2` (pass 1) · ceiling (c) of `NADIA-L1-07`/`PRIYA-L1-02` · `governanceReasons.ts:52-60` | med/high/med | S | `quality-gates/unmeasurable-criteria` | **built 2026-08-31** (`f62cc57a`) — pending recert |
| MC-B37 | **Give an earned zero a word of its own.** `MC-B9` declared two of Priya's four "0 repos" rows *not judged fleet-wide*; `provenance` and `governance` were deliberately excluded from `FLEET_UNJUDGED_REASONS` because the rollup genuinely carries `aiGovernedRate`/`aiPrSample` and the branch-protection fields (`governanceReasons.ts:47-51`). That call is right and its reasoning is **committed only in code** — Priya named all four and will read two rows that changed beside two that did not. One hover on an earned zero naming what was measured to reach it. Honors **G15** (this extends the three-state vocabulary; it does not collapse it). | `RC-N3` (pass 1) · ceiling (b) of `PRIYA-L1-02` | med/high/low | XS | `metrics-rollups/aggregate-honesty` | **built 2026-08-31** (`f62cc57a`) — pending recert |
| MC-B38 | **Pluralize the derived free-scan allowance.** With `PUBLIC_SCAN_MONTHLY_LIMIT=1` the Free card reads *"1 free public scans / month"*, the feature line *"and 1 free public scans"* and the footnote *"The 1 free public scans run on their own rolling 30-day window"*. The 429 already pluralizes (`limit === 1 ? "" : "s"`, `public-scan-quota.ts:367`) — the same care was not applied to the copy that now derives the same number, which is the characteristic cost of making a constant derived. Invisible at 5, visible the moment a self-hosting operator sets 1. **Fix-created defect of `MC-B5`.** | `RC2-N2` (pass 2) · `shots/recert2-B5-pricing-l1.text.txt:44,51,435` | low/med/low | XS | `status-vocabulary/number-formatting` | **built 2026-08-31** (`4567f894`) — pending recert |
| MC-B39 | **The register's worded empty state cannot fire — pick one behaviour and delete the other.** `IndexGallery.tsx:88-90` renders *"No public scans yet. Scan a repository below to be the first on the register."* at `board.length === 0`, but `loadPublicGalleryCards` returns **null** when no cards exist (`scans-read.ts:840`) and the landing page then drops the whole gallery block — observed on a truly empty arm: no heading, no counter, no empty state. Non-null implies `board.length > 0`, so the branch is dead in the exact case it was written for. Present-and-correct-but-unwired; an `MC-M1` member found by an arm constructed to be empty. | `RC2-N3` (pass 2) · `shots/recert2-B8d-zero.aria.yaml` | low/med/low | S | none | **built 2026-08-31** (`e8079720`) — pending recert |
| MC-B40 | **An admission decision must be withdrawable, and the withdrawal must be an act in the ledger.** `/api/org/admission` exposes GET and POST and nothing else; `upsertRepoAdmission` can *change* a decision, so the nearest thing to a revoke is writing `grantedTier == derivedTier` — which still records that an owner decided something. On a route whose own argument is *"an override with no named author is not a decision — it is a measurement with a different value"* (`route.ts:99-102`), the inverse asymmetry is the defect: **a decision made in error is permanent** and the audit trail cannot tell "decided, then withdrawn" from "decided". Add a `DELETE` that clears the state row and writes a **withdrawal** to `OrgAudit` — never a silent row removal. | `RC2-N4` (pass 2) · ceiling of `PRIYA-L2-C5` · found while the pass cleaned up after itself: its own probe row on org `public` could only be neutralised, not removed (pass-2 residue #6) | med/high/high | S | `audit-logging/decision-records` | **built 2026-08-31** (`2a34c40c`) — pending recert |
| MC-B41 | **Stop "closed" meaning three things on the cockpit.** `MC-B33` fixed the per-item verdict; the lane rails still print `{closedIds.length} closed by the rescan` (`LaneRail.tsx:56`, `AutopilotBandParts.tsx:123`) — honest for post-fix lanes, the **raw trailer count** for every pre-fix one — and the outcome sheet header prints *"324 gaps closed"* where `gaps` is `diff.closedGapCount` (`outcomeCellFold.ts:81`), a scan-diff quantity. A reader who has just learned that "claimed resolved" is not "closed" meets "closed" twice more meaning two other things. Rail → *"verified closed"*; header → *"gaps no longer raised"*. **`MC-M2` binds this change by construction.** Honors **G18**. | `RC2-N6` (pass 2) · ceiling of `PRIYA-L1-702` | med/high/high | S | `status-vocabulary/vocabulary-chain-integrity` | **built 2026-08-31** (`0e0d15a4`) — pending recert |
| MC-B42 | **Rubric qualifier on the landing register too.** MC-B18 covered `RegisterEntry` (/leaderboard); the landing `IndexGallery`/`PublicRepoCard` (a different type, cached read in scans-read.ts) still shows no rubric chip on stale rows. Same derivation, second surface. | lane-C handoff (scope deliberately left) | low/med/low | XS | `scoring-rubrics/rubric-stability` | open |
| MC-B43 | **CockpitSetup guide link falls back like MC-B22's surfaces.** `CockpitSetup.tsx` still uses `sourceRepoHref("docs/SETUP.md")` and prints nothing when the env is unset — one-line switch to `docHref()`. | lane-A handoff | low/med/low | XS | none | open |
| MC-B44 | **The outcome column names its engine.** `agentConfig` renders with no engine label (`OutcomeSheetHeader.tsx:66`) — needs an engine fact on the run row nothing records today (claude-cli vs local provider). | lane-B handoff (L2-B-02 carried) | low/med/med | S | none | open |
| MC-B45 | **Lane × team showback matrix.** MC-B19(b) now stamps `teamKey` on local-lane events; once real team cells exist, the matrix view is the read half. Sequenced after data accrues. | lane-A handoff (MC-B19c deliberately left) | low/med/low | S | `cost-metering/spend-attribution` | open |

### Concept-doc (design questions remain, so write, don't code)

| # | Item | Evidence | `standard:` | Status |
|---|---|---|---|---|
| MC-C1 | **The intervention outcome ledger has no producer.** L2 **refuted the L1 diagnosis**: the UI seam exists and the hidden sort toggle plus the API's `sort:"priority"` downgrade are the *same designed refusal* to order by zero measurements. What is absent is the data — no seeder produces outcome rows, no app path inserts them, and `OUTCOME_MIN_SAMPLES = 3` puts the first real measurement three closes away. What instrument writes an outcome row, at what `recommendationMatchKey`, against which scan pair, and what happens at a rubric bump are design questions. **Write `docs/resolutions/intervention-outcome-ledger.md`; extends moonshot #9 (`MC-X4`), does not replace it.** The pure half must not be touched: no median without its `n` and instrument, `null` never `"+0"`. | `SAM-L1-09` · arm B `shots/armB-sam-recs.json` (`expectedLift` on the wire, null on all three rows) · Sam: *"That is a better product than I credited, and it moves the finding from 'wire the seam' to 'there is no path to ever produce an outcome row.'"* | `adoption-measurement/before-after-outcome-pairing` | open |
| MC-C2 | **Can "metered" and "capped" be two words on a page a buyer skims?** `MC-B5` gave the free allowance one source across the Free card, the metadata and the FAQ; the **credit matrix further down the same page** was not in the write set. `creditMatrixData.ts:124` still opens Scanning with *"Public scans are always free and never metered."* and the "Public repository scan" row renders `Unlimited` in all four tier cells (`cells: all("Unlimited")`, line 131). The reconciliation exists — *"Never metered on any plan — rate-limited and monthly-capped instead"* — in the row's **detail text, below a cell that says Unlimited, on a page whose Free card says 1**. It is **pinned as intentional** by `creditMatrixData.test.ts:58-59`, so this is a decision to revisit, not a miss: either the matrix cell states the allowance and "never metered" moves to the detail, or the page accepts that a skimmer reads two numbers for one tier. **Write `docs/resolutions/free-allowance-vocabulary.md`; do not delete the test without answering the question it pins.** Honors **G11**. | `RC2-N5` (recertify pass 2) · ceiling of `TOMAS-L1-02` · **convergence: the third run in which a Tomáš-class reader meets two numbers for one free tier** (`B8` 2026-08-10 → `TOMAS-L1-02` this run → its own residue) | `plan-entitlements/price-book-authority` | open |

### Method commitments (standing rules with a trigger, not tickets)

| # | Commitment | Trigger | Origin |
|---|---|---|---|
| MC-M1 | **A lane lands its consumer in the same PR, or names the consumer's backlog id in the PR body. Before merge on any PR adding an exported field, route or env var, run the wiring grep against the UI directory that should read it — a zero hit is a defect by construction, not a TODO.** A docstring asserting a caller is not evidence of one. | Any PR adding an exported field, a route, or an env var. | The **present-but-unwired** class: 22 members across all seven journeys, 17 found by a literal zero-hit grep. Dana: *"When a thing is built, tested, documented as being wired, and isn't — I stop trusting the inventory."* Nadia: *"the design is better than the wiring."* |
| MC-M2 | **Any change to the loop's verdict vocabulary re-runs Priya × `loop-to-l5` before merge.** The words `resolved` / `closed` / `verified` / `in progress` as rendered in the cockpit, the outcome sheet or the backlog. | A diff touching `CockpitVerdicts.tsx`, `lane-outcomes.ts`, `outcomeText.ts` or `recordLaneOutcomes`. | `PRIYA-L1-702`, and the precedent that **M1 worked**: the briefing PDF changed in the moonshot, M1 made Dana's journey an owed commission, and it produced `MC-B1` and confirmed the `RV-B3` strength. |

### Declined (recorded so they cannot resurface without new evidence)

| # | Item | Reason | Reopen when |
|---|---|---|---|
| MC-D1 | Add a trajectory / ETA to the fleet Overview (`DANA-L1-004`, **recurrence 2**) | **The 2026-08-10 decline (`D1`) stands unchanged: its reopen condition has not been met** — `B9`/`MC-B1` has not shipped, and adding a second ETA surface first would multiply the exact defect `MC-B1` fixes. L2 *sharpened* the decline: the fleet has exactly one forecast surface, so `MC-B1` fixes the fleet GPS in place, and the caveated renderer is wired only to `/trends` and the personal workspace — *"the honest version was built and pointed at the audience that does not need it."* | **Half met 2026-08-31**: `MC-B1` has now shipped (`4aa12080`) **and** recertified (pass 1, `DANA-L1-001`/`-002`/`-013` all `resolved-verified`), so the first clause of the reopen condition is satisfied and the objection that motivated the decline — *"adding a second ETA surface would multiply the exact defect `MC-B1` fixes"* — no longer applies. **The decline still stands** on the second clause: no Character has asked for the trajectory on Overview specifically since, and Dana's need is served in place on the Briefing. Reopen the moment one does. |

### Covered elsewhere (recorded so they are not double-entered)

| # | Item | Owned by |
|---|---|---|
| MC-X1 | The briefing narrative bypasses `src/lib/llm/` (`DANA-L1-005`, `DANA-L1-009`, both recurrence 2, both env-gated off ⇒ `uncertain`) | Concept-doc **C3** (2026-08-10 section), still open. **Premise change recorded against it:** the enterprise data-boundary claim is now on the pricing page, so the deferral is no longer free and should be re-confirmed rather than rolled forward. |
| MC-X2 | The `requireChecks` **editor surface** | Moonshot round item **#8**, known follow-up *"requireChecks editor surface"*. `MC-B9` adds only the round-trip/merge semantics, the blind reconciliation and the fleet-count honesty. |
| MC-X3 | The admission **proposal dry-run modal** (`NADIA-L1-09`, artifact half) | Moonshot round item **#8**, known follow-up *"proposal dry-run modal UI"*. Only the disclosure sentence is new (in `MC-B29`). |
| MC-X4 | The intervention outcome ledger as a feature | Moonshot round item **#9**, `shipped-unverified`. `MC-C1` writes the missing producer design against that row. |

### Method / fixture

| # | Item | Evidence | Status |
|---|---|---|---|
| MC-M3 | **A watched repo.** No GitHub App on the UAT host (`autoscan.githubApp:false`) ⇒ `POST /api/org/watch` unreachable ⇒ `recordConformance` returns `recorded:false` ⇒ **no `ControlObservation` row can be created through any app path**. Blocks the numeric halves of `NADIA-L1-01/-02/-03/-06`, the digest Controls block (`DANA-L1-015`) and `PRIYA-L1-S1`'s secrets-provisioning ceiling. **Fixture:** an installed App with `Secrets: write` on a test org, or a direct `WatchedRepo` insert then the same conformance POST. | run's Environment-ceilings table; arm B journal | open |
| MC-M4 | **An outcome ledger.** ≥3 `kind:"recommendation"` outcome rows sharing one `recommendationMatchKey` and one instrument, so `expectedLiftClause` can return non-null. No seeder produces them and there is no app path to insert them — which is itself `MC-C1`. | `SAM-L1-09` ceiling | open |
| MC-M5 | **An org whose slug equals its repos' owner namespace** (seed `xkazm04`, not `kiro`), one repo at a derived tier ≥ T1 *or* `repoGate` taught to consult `listOrgAdmissions`, plus a claude-cli quota window. Closes `PRIYA-L1-701`'s remote half. A **cloud-shaped** deployment (`selfHosted()` false) with an org-owner session additionally closes `PRIYA-L1-703`. | arm C closing fixture, stated in full on the finding | open |
| MC-M6 | **Overlay corrections applied in this drain** (recorded so the next `/uat update` knows what was and was not re-derived): Victor's character sheet repriced 500 → **150** credits/mo; `uat/env.md`'s "no seeded org can produce a forecast" gap **closed** (org `public` + `?range=custom` is the zero-write low-data fixture); `uat/env.md` §Grounding Surface A **marked stale, deliberately not re-derived** — the denominator is a scored instrument and `/uat update` owns it; a new §Arm construction rule pinning the anonymous-arm recipe. `uat/README.md` gained the missing `## Drain homes` section. | analysis doc §3 | done (2026-08-31) |
| MC-M7 | **Overlay corrections applied in the FOLLOW-UP drain** (recertify passes 1+2), recorded so the next `/uat update` knows what was and was not changed: `drive-armB-gatepolicy.mjs` and `drive-armA-dimloop.mjs` now take their shot stem from `SHOT_PREFIX` (env) or a positional argument, **defaulting to the old names** — both had hard-coded stems and destroyed the arm they were reused from, twice, irrecoverably (`RC-M2`, `RC2-M2`); `uat/env.md` §Arm construction gained five standing rules — `rm -rf .next-empty` before an empty-database arm plus the one-instance constraint (`RC2-M1`, a near-miss false pass on `MC-B8d`), *move the source to certify a single-source claim* (`RC2-M3b`), the zero-residue gate probe with its same-origin note (`RC2-M3a`), the @react-pdf text-extraction recipe (`RC-M1`), and the reusable-driver stem rule. **Grounding denominators deliberately untouched** — `§Surface A` keeps its ⚠ STALE banner; re-deriving a scored instrument outside `/uat update` invalidates the cross-run trend. | analysis doc §Follow-up drain §3 | done (2026-08-31) |
